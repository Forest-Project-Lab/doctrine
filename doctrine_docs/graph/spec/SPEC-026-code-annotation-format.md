---
id: SPEC-026
title: コード注釈の書式（対の印・範囲の指紋・走査の対象）
type: SPEC
domain: graph
status: current
owner: doctrine-maintainers
created: 2026-07-27
updated: 2026-07-28
sources: [doctrine_docs/graph/decisions/ADR-054-code-range-scope-expansion.md]
depends_on: [REQ-003]
llm_context: task
---

# コード注釈の書式（対の印・範囲の指紋・走査の対象）

ADR-054 が定めた追跡の終点（注釈の対が囲むコードの範囲）を、機械が読める書式として固める。言語ごとに手段を分けない。構文解析はしない `[R3][R4]`。

## 入出力

- 入力: 走査の根（統治木の親）以下のファイル。
- 返す値: 範囲の一覧・所見の一覧・勘定の三つ。各範囲は `{id, path, begin_line, end_line, fingerprint}`。`path` は根からの相対で、区切りは `/` に揃える（機械をまたいで共有できる形にする）。絶対パスも利用者名も持たない。

### 勘定（保存則。ADR-058）

走査が触れたファイルは、必ず次のどれか一つに数える。**触れたファイル数 ＝ 寄与 ＋ 印なし ＋ 明示管理外 ＋ Σ 除外**（ADR-067）。

- 寄与（annotated_files）: 範囲を一つ以上返したファイル。
- 印なし（unmarked_files）: 読めたが範囲を一つも返さず、統治外の宣言も無いファイル。入れ忘れが住む場所であり、除外ではない。
- 明示管理外（exempt_files）: 統治外の意思を宣言したファイル（ADR-067）。
- 除外（excluded）: 除外規則の正本表（`EXCLUSION_RULES`）の規則 id ごとの件数。除外はこの表を経由してだけ起こる。
- git の無視の判定（gitignore）: 走査の入口で git へ訊いた結果の状態（`read` / `no_git` / `not_a_repo` / `error` / `not_asked`）。黙らないが責めない（ADR-089）。
- 刈り（pruned_dirs）: 降りなかったディレクトリの規則 id ごとの本数。配下は未到達と明示する（件数の対象外）。
- 打ち切り（truncated）: ファイル数上限で走査を打ち切ったか。分類せず落とした既知の分は excluded の枠に数える。

全規則の枠を 0 件でも必ず含める（空欄のセルを許さない。SPEC-025 と同じ原則）。項ごとの一覧（members）は、求められたときだけ導出して返す。既定は件数だけとする。

### 問い合わせ（`trace-index.py`）

索引はファイルに置かず、問い合わせのたびに導出する（ADR-055）。

- 引数は `[--root PATH] [--docs-root PATH] [--id ID] [--coverage] [--term TERM] [--format json|text] [--max-files N]`。
- `--id` を与えると、その仕様に対応する範囲だけを返す（仕様の側から見た逆リンク）。与えなければ全ての範囲を返す。
- `--coverage` は勘定（件数）を返す。`--term` を添えると、その項の一覧（`annotated`・`unmarked`・`excluded:<規則id>`・`pruned:<規則id>`）をその場で導出して返す。`--term` は `--coverage` と共にだけ使え、規則の表に無い項は使い方の誤りとする。
- `json` は `{"schema": "trace-index/1", "root": <相対の基準>, "ranges": [...], "findings": [...]}`。`root` は名前だけを載せ、絶対パスを載せない。
- 終了コードは、問い合わせが成立すれば所見の有無にかかわらず 0、使い方を誤れば 2、根が見つからなければ 3 とする。これは問い合わせのための CLI であって、違反を止めるゲートではない（dep-graph と同じ規約）。

### 記録と宣言（仕様の側）

仕様の文書は `## 実装の指紋` の節で、コードとの関係を宣言する（ADR-061）。

- 追跡を結ぶなら、指紋の行（`- sha256:<64桁>`）を記録する。
- 意図してコードと結ばないなら、`- コード対応なし: <理由>` の一行を書く。宣言した仕様は下向きの照合の対象にならないが、宣言に反して範囲がコードにあれば監査が warn で指す。
- 節そのものが無い現行の仕様は未宣言であり、コードの範囲がそれを指していれば監査が advisory で名指しする（節を消して注釈だけ残る欠陥の可視化）。

### 編集時の促し（ADR-063）

追跡を使っている体系（直近の監査の要約に勘定がある）で、印の無いコード（dot でも `.md` でも統治木の中でもないファイルに、対を成す印が一つも無い）を編集したとき、促しの面（review-nudge。PostToolUse・Level 3 以上）が紐づけの一行をセッションに一度だけ出す。文は両方の道（印の対と指紋の記録／「コード対応なし」の宣言）を示す。読むのは編集された一ファイルと要約だけで、統治木の走査はしない（NONGOAL 第5項）。

### 監査への載せ方

監査は走査が走ったとき（`## 実装の指紋` の節を持つ文書が一つでもあるとき。状態を問わない。ADR-056・ADR-060）、要約に `trace_coverage`（勘定。件数だけ）を載せる。検査所見は増やさない — 版を上げただけで既存の緑の監査が変わらないための段階導入である（ADR-058）。勘定には、キャンペーンの種（未宣言の先頭一件 `next_undeclared`）と停滞計（`stagnation_streak`）も載る（ADR-065。詳細は SPEC-011）。

### 印の書式

一行に印を一つだけ置く。行から前後の空白と非語文字を取り除いた残りが、次のどちらかに一致する行を印とする。

- `doctrine:begin <id>`
- `doctrine:end <id>`

前後の非語文字を無視するので、`#`・`//`・`--`・`;`・`%`・`/*`・`*/`・`<!--`・`-->` のいずれの注釈記号でも同じ規則が効く。言語を知る必要がない。`<id>` は登録簿の書式（`<TYPE>-<NNN>`）とする。

### 統治外の宣言（ADR-067）

行から前後の空白と非語文字を取り除いた残りが「印の語 + `exempt`（以降は任意の理由）」に一致する行を持つファイルは、統治外の意思を宣言したものとする。理由は機械が解釈しない。

- 宣言したファイルは勘定の第四項（exempt_files。明示管理外）に数える。除外ではない — 一覧は `--term exempt` でいつでも導出できる。
- 宣言したファイルに範囲の印（begin/end）もあるときは矛盾（`trace_exempt_conflict`、warn）として挙げる。実態を優先し、範囲は生かして寄与に数える（範囲を黙って落とすと仕様側に第二の所見が連鎖する）。
- 揺れた宣言（印の語の直後に空白を挟む等）は疑い（ADR-059 の拡張）として挙がり、宣言は成立しない。

### 設定の適用除外（ADR-072）

注釈をファイルに書き込めない媒体のための、統治外宣言の第二の道である。生成の種になるテンプレート（書けば生成先へ写り込む）・注釈を持てない JSON・改変しない法的文書が対象になる。

- 設定（`_system/.context-config.json`）の `trace_exempt` に `{パス: 理由}` で宣言する。末尾が `/` のパスは前置きの一致（配下すべて）。パスは走査の根からの相対で、区切りは `/`。
- 走査は該当ファイルを読む前に「明示管理外」（exempt_files）へ分類する。保存則の枠は変えない（ADR-067 の第四項と同じ枠に数える）。
- 理由の無い項目・文字列でない項目は宣言として成立しない（読み飛ばす。宣言なき除外を作らない）。
- ファイルに書ける媒体は in-file の宣言（ADR-067）を正とする。設定側を影の台帳にしない。

### 悉皆モード（ADR-072）

`trace_mode: "exhaustive"` を設定した体系では、「印なし」は未決の残高である。監査は残高が 1 件以上のとき、warn 一件（`trace_unmarked_backlog`）で件数と三つの出口（範囲を張る／ファイル内の exempt 宣言／設定の適用除外）と一覧の導出（`trace-index --coverage`）を告げる（検査の詳細は SPEC-011）。未設定の既定は現状のまま（数えるだけ）。鼓動は、仕様側の悉皆が済み（未宣言 0）印なしが残りモードが未設定の体系に、モードの存在を一度だけ中立に案内する（SPEC-021）。

### 疑いの検出（ADR-059）

厳密な照合はいっさい緩めない。照合に一致しない行のうち、**印の語 `doctrine:` の直後（空白を許す）に `begin` または `end` の語が続く行**を、綴りの揺れの兆候（`trace_marker_suspect`）として挙げる。検出できる変種は、コロンの後の空白・小文字の id・下線の id・行頭の語文字（文字列の中の印の形）・余計な語の付加・id の欠落である。印の語自体の綴り誤りは検出できない（走査は印の語で読む対象を絞る）。散文が偶然この形を踏む誤検出がありうるので、監査は advisory に留める。

印の書式をコードの原文にそのまま書かないこと。試験が守る規律（IMPL-021）を、走査の実装自身と、走査の対象になる全てのコードに広げる。書式の例示は統治文書（`.md`。走査の対象外）だけが持つ。

### 対応付け

- 入れ子を禁じる。開いている間に `begin` が来たら誤りとする。
- `end` は直前に開いた `begin` を閉じる。両端の `id` が食い違えば誤りとする（写し間違い・並べ替えの取り違えを捕まえる。冗長だが安い）。
- 開いていない `end` は誤りとする。
- ファイルの終端で開いたままなら誤りとする。
- 同じ `id` の範囲が複数あってよい（一つの仕様を複数の場所で実装しうる）。

### 範囲の指紋

- 対象は、印の行を**含まない**、その間の行だけとする。印の注釈記号を書き換えても指紋は変わらない。
- 正規化の順は、BOM の除去 → 改行を LF（改行一文字だけの形）へ揃える → 各行の行末の空白を除去 → 末尾の空行を除去 → `utf-8` で符号化。
- `hashlib.sha256` で計算し、`sha256:<64桁>` と記す（EXT アンカーと同じ記法。ADR-039）。

正規化する理由は、内容が同じものを古びと判じないためである。改行コードは環境が混ざれば割れる（同じリポジトリを Windows と Linux の両方で扱うと起きる）。行末の空白は整形器が黙って落とす。EXT アンカーの指紋が生のバイト列なのは、そこでは「外部のファイルが一バイトも変わっていないか」という別の問いを見ているからであり、規則が違うのは意図である。

## 制約

- 標準ライブラリだけで実装する（DECIDED-001 事実7）。構文解析器を使わない。
- 除外は、除外規則の正本表（`EXCLUSION_RULES`）の規則 id を経由してだけ起こる。表に無い除外を作らない。規則を足す・消すときは、正本表と**受入の手書きの期待表**（`EXPECTED_EXCLUSION_RULES`）の両方を同じ変更で更新する（ADR-060 の様式）。**期待表を正本から生成してはならない** —— 生成すれば凍結の意味が消える。以前は枠の一致（勘定の鍵 == 正本表）だけを見ており、規則を足しても黙って通る状態だった（ADR-089 で二規則を足したとき何も落ちなかったことで判明した）。
- 統治木の中は走査しない。文書は追跡の終点にならない。根が統治木と重なる呼び方でも、包含で判じて刈る。
- **git が無視する物は走査しない（ADR-089）。** 判定は走査の入口で一度だけ git に訊く（`git ls-files -o -i --exclude-standard --directory`）。**`.gitignore` を自前で解釈しない** —— 否定・`**`・アンカー・ディレクトリ限定・親からの継承・`.git/info/exclude`・大域の設定を再実装すれば、必ずどこかで git と違う判定を出す。正本を再実装しない。刈りは `gitignored_dir`（配下は未到達）、ファイルは `gitignored_file` として数える（黙って切り詰めない）。追跡下のファイルは `.gitignore` に一致しても走査する（git がそう扱う）。git が使えないとき（git が無い・リポジトリでない・問いが失敗）は、勘定の `gitignore` に状態を残して**いまと同じ挙動へ退く**。所見は出さない —— git を使わない木を毎回叱らない。
- **`.gitignore` と `trace_exempt` は別の事実である。** 前者は「リポジトリに入れない物」、後者は「リポジトリに在るが印を持てない／持たせない物」（持ち込んだ道具・一次文書）。同じ事実を二箇所に書かせない —— ビルドの投影を `trace_exempt` へ写す必要は無くなった（ADR-089）。
- `.md` は走査しない。この書式を説明する文書自身が印として読まれるのを断つ（自己言及を構造で断ち切る）。
- dot で始まるディレクトリと `node_modules` は見ない（監査の体系外 .md 走査と同じ規約。SPEC-011）。ディレクトリへのシンボリックリンクは降りない。いずれも刈りとして数える。
- 通常ファイル以外（名前付きパイプ・ソケット・デバイス）は開かない（開くと戻らないことがある）。開く前に stat で判じ、除外として数える。
- 内容の先頭に値ゼロのバイトを含むファイルは飛ばす（バイナリを読まない）。
- 一ファイルの大きさの上限と、走査するファイル数の上限を設ける。超えたときは飛ばした事実を所見一つで正直に告げ、勘定にも数える（黙って切り詰めない。SPEC-011 の語彙的酷似と同じ作法）。
- 速さのため、印の文字列を含まないファイルは復号せず印なしに数える。ただし「読まずに飛ばす」ことはしない（読んで含まれないと確かめてから数える）。

## エラー時挙動

- 読めないファイル・復号できないファイルは、規則 id で勘定に数えてから飛ばす。走査そのものは止めない（復元力を完全性より採る。§1.10。何を飛ばしたかは勘定が残す）。
- 読めないディレクトリは刈りとして数える。配下は未到達である。
- 印の対応付けの誤り（入れ子・不一致・閉じ忘れ・開いていない `end`）は、走査を止めずに所見として集める。一ファイルの誤りが他のファイルの走査を巻き込まない。
- 走査は決して例外を外へ出さない。

## 実装の指紋

対象は印と疑いと宣言の照合の正本。更新は `trace-index.py --id SPEC-026` が返す行を写す（ADR-061）。

- sha256:40cd012442bc2335ede11258996a5b74757a75c1a4d7ab7f56c163f8fbcce487
- sha256:d2ab67666bb9a45ca5015b1157e7d36e46811bfaddfa2b9b9d23675dcd0fc338
- sha256:fbaad0db88469a98dfa490ce1d97bf0a5f980617f9163d88aa7983d98137e08e

## 受入基準

- 注釈記号の異なる複数の言語（`#`・`//`・`--`・`;`・`%`・ブロック注釈）で、同じ印が同じように読めること。
- 印の行を含めずに指紋を取ること。印の注釈記号だけを書き換えても指紋が変わらないこと。
- CRLF と LF で同じ内容なら、指紋が一致すること。行末の空白の有無で指紋が変わらないこと。
- 入れ子・両端の `id` の不一致・閉じ忘れ・開いていない `end` を、それぞれ別の誤りとして挙げること。
- 統治木の中と `.md` を走査しないこと。バイナリを飛ばすこと。
- 上限を超えたときに、飛ばした事実を告げること。
- 返す `path` が相対で、区切りが `/` であること。絶対パスを含まないこと。
- `trace-index.py` が `--id` で仕様の側からの逆リンクを返すこと。`json` に絶対パスが混じらないこと。終了コードが 0/2/3 になること。
- 勘定の保存則が成り立つこと: 触れたファイル数 ＝ 寄与 ＋ 印なし ＋ 明示管理外 ＋ Σ 除外。
- 統治外の宣言が第四項に数えられ、理由が任意であること。宣言と範囲の印の同居が矛盾（warn）として挙がり、範囲が実態として生きること。揺れた宣言が疑いになり宣言として成立しないこと。
- 設定の適用除外が、読む前に明示管理外へ分類されること。前置きの一致（末尾 `/`）と単一ファイルの両方が効くこと。理由の無い項目が宣言として成立しないこと。保存則が保たれること。
- 悉皆モードの体系でだけ、印なしの残高が warn 一件で挙がること。既定では挙がらないこと。
- 勘定に全規則の枠が 0 件でも在ること。枠の集合と規則の表が両方向で一致すること。
- 名前付きパイプが根の配下にあっても走査が止まらず、除外として数えられること。
- 印なしが除外ではなく独立の項として数えられること（入れ忘れの住処を規則の陰に隠さない）。
- `--coverage` が件数を、`--coverage --term <項>` が当該の一覧を返すこと。一覧は既定で持たないこと。
- 監査の要約に、走査が走ったときだけ `trace_coverage` が載ること。opt-in が無ければ載らないこと。
- 綴りの揺れた印（コロンの後の空白・小文字/下線の id・行頭の語文字・余計な語・id の欠落）が疑いとして挙がること。厳密な照合の答え（範囲を作らない）は変わらないこと。
- 正しい印・印の語だけの散文・コロンの直後が空白でも `begin`/`end` でもない行（照合の原文など）が、疑いにならないこと。
- リポジトリ自身を走査して、疑いがゼロであること（実装・試験の原文に印の形を書かない規律の凍結）。
- 受入は TEST-026 が凍結する。

<!-- 入れない: 廃止、検討、実装コードの写し -->
