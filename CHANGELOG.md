# 変更履歴

本ファイルは doctrine プラグインのリリースごとの変更を記録する。「なぜ」の根拠は各 ADR に、詳細な差分は git 履歴にある。版番号の正本は `plugin/.claude-plugin/plugin.json`。開発中の変更は「未リリース」節へ積み、リリース時に版番号と日付を付ける。整合は CI の門(`scripts/release-check.py`。SPEC-027)が検める。

## [未リリース]

（次のリリースへ積む変更をここへ）

## [0.11.0] — 2026-08-09

### 足した（配布 8 個目の技能 system-map-draft）

- **配布技能に `system-map-draft` を加えた**（ADR-136。ADR-010 を置換）。対象リポジトリから
  System Map の意味モデルの下書き（proposed 限定の候補）を起草する技能である。doctrine-lens
  の Phase 2（issue #212）が doctrine 側の領分と定めた書き込み系の道具で、確定（confirmed への
  昇格）は人が行う。技能の数の固定はやめ、**一覧の正本は SPEC-016、増減は根拠 ADR の置換のみ**
  とした（DECIDED-001 事実8 を言い直し）。doctrine の型・必須キーは増えない（スキーマ不変は維持）。
- **出所の機械検証 `map-draft-check.py` を加えた**（SPEC-029・TEST-029）。下書きの全値の出所
  （読んだ場所・確認日・判定）が実在するかを機械で検める —— 捏造出所ゼロが検収の門。外部 URL は
  取得せず「機械検証不能」として列挙する（検証可能性を偽らない）。依存グラフの辺を Flow へ
  変換した形も落とす（M-08 の早期信号）。

### 直した（利用者の辞書と節名の袋小路を機構で解いた）

- **必須節の名の覆いを登録簿から導くようにした**（#197。ADR-135）。利用者の辞書が節名の
  語（『影響するテスト』の『テスト』など）を禁じると、節名を書けば禁止同義語・書かなければ
  節の欠落と、二つの ERROR が互いを打ち消して書き手に逃げ道が無かった。節名の正本は
  登録簿（`_registry.REQUIRED_SECTIONS`）であり書き手は言い換えられない —— 言い換えられ
  ないものを照合に掛けない。登録簿が節名を増減すれば覆いが自動で追随し、手写しの一覧は
  登録簿が知らない雛形語彙だけを残す。性質の試験（全型・全節名）で凍結した。

### 直した（潜在 NameError の型を全数監査で除いた）

- **例外で守られた import が失敗時に名を束縛しない潜在形を除いた**（INC-007。issue #208 の
  現地報告「不活性に見える行の NameError」を生み得る唯一のコード内の型）。`_termcheck` は
  `_registry` の import 失敗時も `_registry_mod = None` を必ず束縛し、覆いは手書きの一覧と
  辞書由来の源だけへ退く。`docs-linter` の未使用の退避 import（`_depgraph`）は削除した。
  形はメタ試験（AST 全走査）で凍結した。**現地の根本原因は不明のまま**であり、事象は
  UNKNOWN として台帳に保持する（ADR-115。再現不能は実装へ進まない）。

### 足した（例ではなく性質で守る回帰の門）

- **用語チェッカーの偽陽性コーパス試験を本体試験へ入れた**（INC-004 の事故分析の推奨）。
  守るのは例ではなく**性質**である ——「点検器が指摘した語は、原文に**単独の語として
  実在**しなければならない」。長い綴りの一部を切り出して「未定義語だ」と言う形の誤反応は
  **INC-004（ハイフン。`NOT-APPLICABLE` の `NOT`）と INC-011（ドット）で二度**起きており、
  戻してはならない事項（WATCH-001 第2項）に挙がっている。従来の試験は挙げた例しか
  守らなかったので、**例に挙げていない綴りでも赤になる**形へ引き上げた。コーパスは体系が
  現に使う識別子で組む。**この試験が現に守っていることは故障注入で確かめた** ——
  識別子の断片を除く判定を無効化すると赤になり、戻すと緑になる（`plugin/tests/
  test_termcheck_corpus.py`）。

### 足した（現地で何が動いているかを、送らずに渡せるようにした）

- **`gov-heartbeat.py --field-state` が、現地の状態を貼り付けられる平文で出すようにした**
  (ADR-122)。規範（CAST ハンドブック）は「運用側は運用中の性能を製造者へフィードバック
  する」と求めるが、本プラグインは通信しない（確定事実7）。そこで**送らずに転記できる
  形**にした。載せるのは版と生存の印だけ —— 実行中の版・冒頭に刻まれた版・正本との
  食い違いの有無・監査の走った印・要約を書けたかの印・リンタとガードの印。
  **統治対象の内容（文書の場所・本文・所見の中身）は載せない**（エラージャーナルと
  同じ線引き）。読めない項は「不明」と書き、推測で埋めない。**送信はしない** ——
  渡すかどうかは利用者が決める。あわせて、版の遅れを告げるときだけ求め方を助言へ添える。
  受入は SPEC-021・TEST-021 が名指しする3件。

### 変えた（決定の置換。沈黙して 0 を返さない）

- **監査は、要約を書けなかったとき終了コード 3 を返すようにした**(ADR-121)。
  以前は「要約の書き込みに失敗しても 0 を保つ」と仕様に明示して決めていたが、
  **書けたときと外から区別がつかない**。統治の唯一の全体像を書けなかった実行が
  成功として終わる形は、確定事実12「フックの境界は沈黙して開かない」と正面から
  食い違う。規範の根拠は、送ったコントロールアクションが守られたと仮定しない・
  フィードバックの欠如が欠陥のあるプロセスモデルを作る・提供しない状態を仮定で
  片付けない（いずれも STPA ハンドブック）。所見の重さとは分け、前提の欠如の規約
  （3）へ倒す。**要約そのものと画面への表示は従来どおり出す** —— 止めるのは沈黙
  であって監査の本務ではない。旧決定を凍結していた受入
  （`test_atomic_write_failure_still_exit_zero`）は、置換の記録つきで書き換えた。

### 足した（沈黙していた失敗を名指しできるようにした）

- **監査が走ったこと自体の印を残すようにした**(ADR-119)。要約の書き込みに失敗しても
  終了コードは 0 のまま（仕様が明示して決めている）なので、失敗は外から見えない。
  結果として鮮度の警告は「監査が動いていない可能性がある」としか言えず、
  **『走らなかった』と『走ったが書けなかった』を 8 日ぶん区別できなかった**
  （実測: 本リポジトリの要約が 2026-07-28 から更新されず、版の遅れという別の疑いへ
  時間が費やされた。後に反証 —— 三世代前の配布物も同じ配線を持っていた）。
  `--summary-out` を指定された実行は `hook_session_end_audit`（時刻）と
  `hook_session_end_write`（`ok` / `failed`）を `.claude/.cache/hook-stamps` へ
  最善努力で残し、鼓動は走った証跡が在れば原因の側を名指しする。
  **印が無ければ何も言わない** —— 段階や設定で SessionEnd の監査を持たない構成が
  あり、不在は不実行の証明ではない（ADR-062 と同じ前方寛容）。判定は
  `_auditcache.audit_write_gap` に一度だけ置く。受入は SPEC-021・TEST-021 が
  名指しする3件。**SessionEnd がそもそも発火しない場合は依然として区別がつかない。**

### 直した（照合が意図より広かった）

- **用語チェッカーの未定義語判定が、約物で綴じた識別子を全体として扱うようにした**。
  語境界は約物で切れるので、変更前は `INC-005`・`NOT-APPLICABLE`・`SHA-256`・`v0.7.0`
  の前半が単独の未定義略語として挙がった（実測: 保証キャンペーン 2026-08-05、本
  リポジトリの ADR を書いている最中に**二度**発火。ハイフンが事象 INC-004、
  ドットが INC-011）。下の ASCII（英数字だけの文字集合）語境界の項と同じ
  WATCH-001『部分文字列の取り違え』類型で、こちらは未定義語の側。
  **接着する約物は事例ではなく軸として持つ** —— 正本は `_termcheck.py` の `_GLUE`
  の一行で、足すときは先に受入の行（`test_identifier_glue_axis_is_covered`）を足す。
  一つずつ事例を潰す形が三例目を生んだ。ここに無い約物は従来どおり語の境目に残す
  （緩和を広げすぎないことを `test_standalone_acronym_next_to_punctuation_still_flagged`
  が凍結する）。修正前に FAIL（走ったが不適合）する証拠は
  `assurance/ledger/red/INC-004-undefined-term.json` と
  `assurance/ledger/red/INC-011-dot-glue.json`。

- **用語チェッカーの禁止同義語照合が、ASCII 純字の同義語に ASCII 語境界を要求するようにした**。
  日本語に語境界は無いので生の部分一致が正しいが、ASCII には境界が在る。変更前は
  『IF』(→ICD)が ASCII 語の内部へも一致し、`VERIFY`・`UNVERIFIED` と書いた文書が
  咎められた（実測: 保証キャンペーン 2026-08-04、体系のビュー2件で発火）。WATCH-001
  『部分文字列の取り違え』類型（『入出力』『選択肢』と同族）の新事例。承認複合語の
  覆いへ英単語を足す対処は取らない（覆いは雛形・仕様が定める語に限る。ADR-082）。
  単独の『IF』・CJK 隣接（外部IF・IF仕様）は引き続き咎める。受入は
  `test_ascii_synonym_not_matched_inside_ascii_word` ほか2件が凍結。

- **投影ドリフト検査の列挙判定を、各行の最初の id だけから採るようにした**(ADR-113)。
  変更前は投影本文の**すべての** id トークンを「列挙された文書」と数えており、後継
  ADR の題名「（◯◯を置換）」が Overview の題名セルへ写ると、現行でない文書が
  「載っている」という偽陽性 error になった（実測: 本リポジトリの後継 ADR で発火し、
  題名から旧 id を外して回避していた）。参照検出の共用器（dead link・着地検査の入力）は
  変えていない。受入は TC-136・TC-137 が凍結し、WATCH-001 第12項が退行を見張る。

### 直した（門が在るのに効いていなかった）

- **リンタの一括点検が、`--batch` の後ろを使い方として解くようにした**(ADR-110)。
  変更前は**次の語を無条件に統治木の場所として飲んで**おり、旗の綴り違いで門が
  静かに無効になった。実測: `--batch --root doctrine_docs` は
  **「統治木が無い(--root)」と言って 0 を返していた。**
- **利用者側（doctrine-lens）がこの形のまま門を回していた。** 正しく呼び直したら
  85 文書に 241 件の ERROR が出た（報告より）。**門は在ったが、効いていなかった。**
- **同梱の可搬性の手引きが、その形を雛形として配っていた** —— pre-commit の例が
  `--batch doctrine_docs` と書いており、木の名が `docs/` の導入先では実在しない場所を
  指す。実測: 同じ木で雛形どおりなら 0、`--batch .` なら 8 件の ERROR。雛形を直した。
- 終了コードを兄弟の門（全件監査・追跡の索引・依存グラフ・語の抽出）の規約へ揃えた
  —— **知らない旗と余分な語は 2、場所が実在しなければ 3。**
  **実在するが統治木でない場所だけが 0 に残る**（素の `docs/` を CI で誤って落とさない
  配慮は、もともとそこを指していた）。
- **点検した文書の数を必ず出すようにした。** 変更前の「ERROR なし」は、85 文書を見て
  無かったのか一つも見ていないのかを区別しなかった。
- 保証限界: **利用者側が終了コードを読むかどうかは、この体系の外である。**
  言うところまでが保証で、読ませることはできない。

### 直した（黙って切り詰めていた）

- **上限を超える封筒を切り詰めたら、標準エラーへ一行で告げるようにした**(ADR-109)。
  ADR-108 が保証限界に残した面である —— **上限は掛かるようになったが、切り詰められた
  ことは誰にも伝わらなかった。** 実測: 9 MB の封筒で**三本とも標準エラーは空**。
  使う人から見れば、Hook が理由もなく助言を出さなくなる。
- この体系は「黙って切り詰めない」規律を三度立てている（走査の切り詰め ADR-059、
  語彙的酷似の省略 SPEC-011、`.gitignore` による刈り ADR-089）。**封筒だけが例外だった。**
- **不具合のジャーナルへは記録しない。** あれは「部品が実行時に倒れた」記録であり
  (ADR-074)、**切り詰めは倒れではない**（名が事実を語らなくなる）。
- 返す値は変えない（空の写像のまま）。呼び手は封筒が無いときと同じ道を通る。
- 保証限界: **標準エラーが使う人に見えるかは実行環境が決める。** 保証するのは
  「書くこと」までである。

### 直した（正本の上限を写しが落としていた）

- **Hook の封筒（標準入力の JSON）の読み取りを共有コアに一本化した**(ADR-108)。
  正本 `_hookio.read_payload` は**読む量に上限を持つ**（8 MB）のに、**三本の Hook が
  自前で `sys.stdin.read()` していた** —— しかも**三本とも共有コアを既に取り込んで
  いた**（封筒の読み取りだけを自前で持っていた）。
- **実測: 9 MB の封筒を三本とも全部読んだ。** 前後比較も取った —— 封筒に文書の道を
  入れて渡すと、**変更前は助言が出て（261字）、変更後は出ない（0字）**。上限で切られ、
  封筒が無いときと同じ振る舞いへ退く（三本とも解析に失敗したら空を返す設計なので、
  **degrade の道は既に在った**）。
- 日付(ADR-099)・正規化(ADR-101)・設定の読み取り(ADR-104)・読み取りの門(ADR-107) に
  続く**同じ写しの類型の五件目**であり、**四件目と同じ形** —— **正本が持つ守りを、
  写しが落としていた。**
- 保証限界: **上限の 8 MB は見立てである**（正本の既定をそのまま使う）。そして
  **切り詰められたことは呼び手に伝わらない** —— 正本は空を返すだけで、「封筒が無い」と
  「大きすぎた」を区別しない。**黙って切り詰めない規律（ADR-058）と当たる面**なので、
  別に扱う。

### 直した（寛容に読む一本が門を迂回していた）

- **読み取りの門（ADR-075）を迂回する経路が一本在った**(ADR-107)。同名関数の走査で
  `_read_text` が三箇所に見つかり、**二本は正本へ委ねる薄い前面、一本だけが自前で
  `open`** していた。語の抽出は統治木を走査して `.md` を一つずつ渡すので、
  **`.md` の名を持つ非通常ファイルが在れば、そこへ直に当たる。**
- **ただし寛容さは意図されたものだった** —— 復号できない字を置き換えて走査を続ける。
  語の頻度を数える処理には妥当で、厳密に落とすべきではない。**方針が違うことは、
  門が要らないことではない。**
- **復号の方針を正本の引数にした。** 既定は厳密（いまの挙動）。寛容に読みたい呼び手が
  それを渡す。**門は方針に依らず必ず通る。** 薄い前面は消した（前面は後からそこで
  挙動を変えられる場所になる —— 実際、三本のうち一本がそうなっていた）。
- **今回は門が効くことを振る舞いで測れた** —— 非通常ファイルを渡したときの文言が
  素の `open` の例外から**門の文言へ変わる**。文言が変わることが、門を通った証拠である
  （名前付きパイプで戻らないことは依然として測れない。そこは構造で凍らせる）。

### 直した（使われない正本が静かに嘘になっていた）

- **登録簿に、どこからも使われない公開名が五つ在った**(ADR-106)。走査した ——
  公開名 44 のうち、スクリプトからも登録簿自身の取得関数からも参照されないものである
  （`SYSTEM_TIER_TYPES`・`ALWAYS_CONTRACT_TYPES`・`LEVEL3_KEYS`・`LEVEL4_KEYS`・
  `required_keys`）。
- **そして二つは、生きた規則と食い違っていた。** `ALWAYS_CONTRACT_TYPES` は
  `OVERVIEW` を欠き（既定が `always` の型は五つ）、`SYSTEM_TIER_TYPES` は `REQ` を
  欠いていた。**後者は ADR-091 の帰結である** —— `_system/` に `REQ` を置けるように
  したとき、**誰も読まない表は誰も直さなかった。**
  **消費者が無いことは無害ではなく、静かに嘘になる。**
- `required_keys` はもう一段悪かった。**ICD で公開しているのに生産の呼び手ゼロ**で、
  しかも**その規則の写しがリンタの中に在った**（「DECIDED と WATCH は `review_by` も
  必須」が二箇所。**正本の側が死んでいた**）。公開していた署名は
  `required_keys(level, type)` で、その `level` は**受け取って検証したあと無視**される
  （説明文自身が「level は結果を変えない」と書いていた）。
- 四つの表を落とし、`required_keys` は残して**リンタがそれを呼ぶ**ようにした
  （確定事実1 —— 構造規則は登録簿に一度だけ）。`level` の口は落とした ——
  **何も決めない口を公開しない。**
- **段の梯子は規則ではなく説明である。** 段2で `depends_on` を書くのは違反ではなく、
  **まだ意味を持たない**だけ。禁じるものが無いので検める対象が無い。説明は仕様の
  散文へ移した（強制すると、段を上げようとしている木を叱ることになる）。
- **今回の欠陥を見つけた走査そのものを門にした** —— 登録簿の公開名が消費者を持つことを、
  メタの受入が検める。

## [0.10.0] — 2026-08-03

### 直した（較正が片方の上限にしか効いていなかった）

- **トークンの見積りと較正の解釈を共有コア `_tokens.py` に一度だけ置いた**(ADR-105)。
  `collect-context` の説明文は「較正が要る導入先は設定の `model_chars_per_token` を
  下げる」と約束していたのに、**読んでいなかった**（呼び出し二箇所とも既定 4.0）。
  読むのは `inject-contract` だけだった。
- **実測: 較正 2.0 の木で、同じ 1000 文字を注入は 500、パックは 250 と見ていた。**
  パックの上限は**未較正の見積り**で判じられており、較正した木では意図の二倍が入る。
  **較正はモデルの性質であり、上限ごとの性質ではない** —— 上限そのものは別々のまま
  （確定事実6 は守る。**分けるのは上限であって較正ではない**）。
- **壊れた較正の値への頑健さも違った。** `0` で例外、**`-1` で -1000（負のトークン数）**、
  `'x'` で例外。**負がいちばん悪い** —— 上限との比較が必ず通るので**上限が黙って無効に
  なる**。較正を読ませると同時にその道が開くので、先に頑健な側へ揃えた（零・負・非数・
  真偽値・無限はすべて既定へ退避）。
- 日付の解釈(ADR-099)・スカラへの正規化(ADR-101)・設定の読み取り(ADR-104) に続く
  **同じ写しの類型の四件目**である。**機械が写しを咎める**ようにした。

### 直した（設定の読み手が五つに割れていた）

- **統治の設定の読み取りを共有コア `_config.py` に一度だけ置いた**(ADR-104)。
  この一枚は常時投入の上限（確定事実6）・パックの上限・追跡の悉皆の様式・走査の適用除外を
  握る（ADR-096 で指紋の見張りを付けた、あの一枚）。**読む実装が五箇所に在り、
  そのうち一つだけが `utf-8` で開いていた。**
- **BOM を一つ付けるだけで、監査が設定を丸ごと見失った**（実測）。`trace_mode: "exhaustive"`
  が無視され、**印なしの残高の警告が黙って消える**（警告が消えたことは、警告では
  分からない）。調整値も既定へ戻る。しかも鼓動はまだ悉皆を見ているので、
  **二つの読み手が木の様式について食い違っていた。**
- 日付の解釈(ADR-099)・スカラへの正規化(ADR-101) に続く**同じ類型の三件目**である。
  `ADR-053` が「要約の読み取りを共有コアに一度だけ」と決めた後も、**その中で五本が
  残り続けていた。**
- **数え漏らしそのものが問題だった** —— 数え始めたときは四つだと思っており、
  五つ目（`trace-index`）は探して初めて出た。**機械が写しを咎める**ようにした
  （設定の名を文字列定数で持つスクリプトが在れば、メタの受入が落ちる）。
- 副産物: `gov-heartbeat` に**到達しない残骸**が在ったのを見つけて落とした（前の関数の
  `return` の後ろに紛れ込んでおり、構文としては通っていた）。監査の `_load_config` は
  読まなくなったので `_resolve_knobs` へ改名した（**名が実態を語るようにした**）。
- 通常ファイルの門（ADR-075）が設定の読み取りにも掛かるようになった。**ただし
  名前付きパイプで戻らないことは測れない**（測ろうとすると試験が止まる）ので、
  「共有の読み手を呼び、素の `open` を持たない」ことを**構造で**凍らせた ——
  最初に書いたディレクトリの試験は**門の証明になっていなかった**（素の `open` でも
  同じく黙ることを実測した）。

### 測った（穴が無いことも凍らせる）

- ADR-102 が「他の経路は測っていない」と書いた三つを測り、**どれにも穴が無いことを
  確かめて凍らせた**（2026-08-03）。一覧で書いた `status` でも **Bash の削除**の逆依存の
  判定は狂わず（グラフ側で判ずる）、一覧で書いた `type` でも**越境依存**の判定は狂わない
  （依存先のドメインは置き場所で解く）。PostToolUse の突き合わせが `Edit|MultiEdit` だけを
  扱うのは仕様の入出力に書かれた設計である（`Write` は PreToolUse が全文を事前判定済み）。
  **測っていないままにすると、後から静かに開いても気づけない。**

### 直した（不変ガードが黙って開いていた）

- **受理済み ADR の本文が書き換えられた。型を一覧で書くだけでよかった**(ADR-102)。
  `type: [ADR]` → allow ／ `type: ADR` → deny。同じことが `status: [archived]` でも起き、
  **倉庫の外に居るアーカイブが編集できた**（ADR-027 が閉じたはずの穴の別の入口）。
  確定事実12「境界は沈黙して開かない」の破れである。
- 原因はスカラへの正規化が入れ物に内部表記（`"['ADR']"`）を返し、`"ADR"` と一致しない
  ことだった。**その関数は四箇所に写しが在り**、2026-08-03 に入れ物の欠陥を直したときに
  **一箇所しか直っていなかった**(ADR-101)。**正本を `_frontmatter.coerce_str` に一度だけ
  置き、写しを消した**（薄い前面も置かない）。**機械が写しを咎める** —— 手で書いた表
  （関数名 → 正本のモジュール）で凍らせた。ADR-099 の日付と同じ形で、これが二件目である。
- **当初この ADR には「正規化を揃えるだけでは直らない」と書いたが、測ると誤っていた** ——
  型については、正本化と既存の「id の接頭辞へ退避」で塞がる。**芯は `status` だった**
  （位置づけには退避先が無い）。切り分けて実測し、決定の側を事実へ合わせた。
- それでも**一段手前で止める** —— `type`・`status` が在るのに文字列として読めないなら、
  その場で拒否する。**退避が正しく働くかどうかに、不変を依存させない。**

### 直した（不変ガードが他人の土地を裁いていた）

- **統治木がどこにも無い土地の `type: ADR` なメモが編集できなかった**(ADR-103)。
  ADR-036 は「不変性ガードは統治木の中の archive/ADR にしか効かないため元から無影響」と
  書いていたが、**測るとそうではなかった** —— ADR-036 が名指しした害（他体系の編集を
  誤って拒む）そのものが、不変ガードで起きていた。
- フロントマターを読む二つの経路（既存 ADR の改変・`status: archived` の不変）を、
  **二・三のガードと同じ境界**（プロジェクトに統治木が在るか）へ揃えた。
  **木が在れば、木の外の逸れた文書は従来どおり守る**（緩めたのは木の無い土地だけ）。
  ADR-036 の決定は生きているので置換しない —— **足りていなかった側を足した。**

### 直した（日付の正本が無かった）

- **日付の解釈を共有コアに一度だけ置いた**(ADR-099。`_frontmatter.parse_date`)。
  **測ると四箇所に写しが在り、そのうち一つが違う答えを返していた** ——
  `_auditcache` の正規表現に終端の錨（`$`）が無く、**`2026-01-01xyz` を日付として
  受けていた**。要約の鮮度を読む側だけが緩く、**ADR-053 が一本化した「要約の読み取り」の
  中で、日付の答えが読み手ごとに割れていた**。日付の正規表現は五箇所に在った。
- **薄い前面すら置かない。** 呼び手は正本を直に呼ぶ（前面を置くと、後からそこで挙動を
  変えられる）。**機械が二重定義を咎める** —— 日付を解す関数も日付の正規表現も、
  正本の外に在ればメタの受入が落ちる。
- 副産物: 分類の記録の刻印が**実在しない日付**（`2026-02-30`）を通していた。形だけを
  見ていたためで、正本を通したので消えた。

### 直した（壊れた日付が名を偽っていた）

- **壊れた日付を `bad_date` の名で咎めるようにした**(ADR-100。リンタの `BAD_DATE`／
  監査の `bad_date`)。`updated: 2026-13-45` を持つ文書に対し、**リンタは一件も言わず、
  監査は `stale_current`（陳腐化の疑い）と言っていた** —— 検出ではなく取り違えである。
  読み手は「更新が古い」と読むが、実際は「日付が壊れている」で、直す先が違う。
- **同じ取り違えをもう一つ直した。** 壊れた `review_by` は `review_by_overrun`（超過）の名で
  error になっていた。**形式の誤りは超過ではない。** `review_by_overrun` は超過だけを
  見るようにした。
- **鮮度の検査は、日付が読めないときに黙るようにした。** 以前は「不明は安全側 = 古び」を
  凍結していたが、**その安全側は名を偽っていた**。`stale_draft`・`stale_proposed`・
  `orphan`・`stale_current` が鳴らなくなり、**一つの欠陥が一つの名前で出る**
  （安全側は `bad_date`（error）が保つ。warn より重い）。
- 重さ: `updated`・`review_by` は error、`created` は warn（必須キーではない）。
  **この配分は見立てであり、測って出した数ではない。** いまの木に不正な日付は 0 件、
  呼び手の木でも 0 件なので、所見は増えない（実測）。

### 足した（雛形の埋め忘れを咎める）

- **フロントマターに雛形の指示文が残っていたら咎めるようにした**(ADR-098。リンタの
  `PLACEHOLDER_VALUE`／監査の `template_placeholder`、いずれも error)。
  報告のとおり、**埋め忘れはどの検査にも掛からなかった** —— 題名・責任者・更新日・出所を
  すべて指示文にした仕様を書いても、リンタは**一件も所見を出さなかった**（実測）。
- **判定は形で行い、雛形を読まない。** 報告の提案は「雛形から引く」だったが、**ADR-090 が
  同じ案を却下している** —— 雛形は配布物で導入先で差し替えられ、正本が動くと検査の意味が
  木ごとに変わる。**山括弧の形そのものが指示文の印**であり、実測でいまの木の
  フロントマターに山括弧を含む値は **0 件**なので誤検知の余地が無い。
- **本文は見ない。** `<svg>`・置き場所の記法（`<domain>/`）・id の書式（`<TYPE>-<NNN>`）が
  正当に出るからである（報告自身がそう書いている）。
- **判定を共有コアに一度だけ置き、リンタ（予防）と監査（検出）が同じコアを呼ぶ。**
  監査にも置いたのは、**呼び手が全件リンタを回していない**ためである（`docs:check` は
  監査だけ。doctrine-lens#12）。**報告された実害が直らない置き方はしない。**
- 節点に `owner` を足した。**必須キー8個のうち集めていない最後の一つ**であり、
  題名（ADR-087）・出所（ADR-097）に続く**三件目**だった。**四件目を作らないために、
  必須キーの全部が節点に在ることを機械が凍らせる。**

### 変えた（偽りの緑をやめた）

- **「初期化直後のコーパスは監査の所見ゼロで通る」の凍結をやめた**(ADR-098)。
  初期化したての木を作って測ると、**3 文書に 8 件**の指示文が残る
  （`DECIDED-001`・`GLOSSARY-001`・`NONGOAL-001`）。**報告が見つけた `owner: <記入>` は、
  初期化の瞬間に生まれてそのまま残っていた。** つまり**その緑は偽りだった** ——
  用語辞書の正本に責任者が居ないまま緑だったのである。
- 新しい約束は「**人が埋める箇所だけを名指しして赤い**」——(1) 指示文以外の所見は一件も
  出ない（投影ドリフト・孤児・逆孤児のような道具の側の不備が無い）、(2) 指示文の所見は
  `_system/` の正本に限る、の二つを凍らせる。**埋まっていない正本を健全と呼ばない。**
  一度きりで直す先を名指しする所見なので、**門が邪魔者になる形ではない**と判じた。

### 足した（宣言した出所に機械の目を入れる）

- **`sources` が指す道の実在を検めるようにした**(ADR-097。`source_missing`、WARN)。
  **測ったところ、`sources` は一件も検められていなかった** —— 名前の似た `source_drift` は
  `depends_on` の端の日付を見るだけで `sources` を読まない。中身の実態は 322 件で、
  **59%（190件）がリポジトリ内の道**だった（残りは自由文 19%・文書 id 7%・URL 7%・
  issue 5%）。
- **この検査が無いあいだに 6 件壊れていた。** `RESEARCH-001` が `archive/` へ退避したのに
  五本の ADR が旧い道を指し続け、`ADR-047` は改名されたのに一本が旧名を指していた。
- **ADR は対象にしない。** 受理済み ADR は不変で `sources` の道を直せず、**咎めても直す道が
  無い門は邪魔者になる**（非目標 第12項が「旧 id を指し続ける参照は監査が事後に指す」と
  立場を決めており、`source_drift` と孤児の検査も同じ理由で ADR を除いている）。
  **つまり上記の 6 件はこの決定では直らない**（記録として残す）。ADR を除くと今日の壊れは
  0 件なので、所見は増えない（実測）。
- 節点が `sources` を運ぶようにした。**必須項なのに集めていなかった** ——
  ADR-087 が名指した「必須項が集められてさえいない」欠陥の二件目である（一件目は題名）。
- **認識の等級は欄にしない。** RFC 第1節は `observed`/`inferred`/`proposed`/`unknown` を
  必須の欄にせよと言うが、**機械が見られるのは欄が埋まっていることだけで、値の当否は
  見られない**。値を検めない欄を必須にすると、埋めれば通る儀式になる（非目標 第14項の
  判断と同じ）。散文で書く様式は `RESEARCH-004` が示しており、**機械は検めないことを
  仕様に明記した** —— 検めない範囲を主張しないためである。

### 足した（統治の設定を見張る）

- **統治の設定 `_system/.context-config.json` を指紋で見張るようにした**(ADR-096・`EXT-005`)。
  測ると、この一枚は**五本のスクリプトが読み**、**常時投入の上限（確定事実6）・追跡の悉皆の
  様式・走査の適用除外**を握っていた。静かに書き換えると上限が動き、残高の警告が消え、
  任意の道が走査から外れる。**確定事実12 は「境界は沈黙して開かない」と定めるのに、
  境界を決める設定そのものが沈黙して開いていた。**
- **新しい検査もコードも足していない。** 見張る機構は既に在り、この一枚だけが乗って
  いなかった —— `EXT-002` は `.claude/settings.json` を、`EXT-004` は継続的結合の定義を
  既に繋いでおり、**外側の設定は繋いであって統治の設定だけが繋がれていなかった**。
  雛形は対象の例として「CI の定義、実行環境の仕様」を挙げており、**設定ファイルは
  最初から想定された対象だった**。
- 実物で確かめた: 適用除外を一つ広げると `ext_anchor_broken` が期待と実際の指紋を並べて
  warn を出し（**副作用として `trace_missing_impl` が 17 件出た** —— 適用除外を広げると
  指紋を宣言した仕様が実装を見失う。独立した第二の信号である）、戻すと消えた。
- **アンカーそのものが消されても監査は何も言わない**（見張りが無くなった状態は、
  見張りでは分からない）。そこで `release-check` に見張りの見張りを置いた。
  **最初は同梱の試験に置いたが、既存の門が捕まえた** —— ADR-075「同梱の試験は配布物の外を
  素で読まない」に反していた。**そして最初の版は飾りだった** —— 本文全体で `hash` を
  探していたので、散文の「`hash` にする」で通った（実測）。監査と同じ `検査:` の行を
  読む形へ直した。三通りの退行（アンカーを消す／検査を `exists` に落とす／指紋の行を
  消す）で、それぞれ別の違反文で落ちることを実測した。
- **拒否は入れていない。** RFC の逐語は「打ち直すまで守られた操作を拒む」だが、拒むには
  attestation の信頼根が要る（ADR-093 で断った理由と同じ）。**拒否の代わりに、人の目が
  一度入ることを保証する** —— 打ち直しは差分に現れる。

## [0.9.0] — 2026-08-03

### 直した（語彙は在るのに使えなかった）

- **ADR の不変を `accepted` から始めるようにした**(ADR-095)。`proposed` の ADR は下書きなので
  本文を直せる。**語彙は最初から `proposed` を許し**（`status_allowed("ADR")`）、
  **carve-out も `proposed→accepted` を明記していた**のに、不変ガードが `status` を見ずに
  **存在した瞬間から凍らせていた**。結果、木に `proposed` の ADR は**一件も生まれなかった**。
  痛みは記録されていた —— ADR-044 が「Wave 群の実装中に**何度も遭遇し、いずれも削除して
  作り直すで回避した**」と書き、そのとき採られた決定は**迂回路の明文化**だった。
  2026-08-02 から 03 にかけて、こちらでも三度払った（一本の ADR を三回書き直した）。
  **棚は在って、乗れなかっただけである**（ADR-091・ADR-092 と同じ形）。
- **逆向きは開いていない。** `accepted → proposed` の降格は carve-out の外に留まる
  （唯一の穴になりうる経路なので、拒まれることを実測し試験で凍らせた）。
  `archive/` と `status: archived` の不変も変えていない。
- `stale_proposed`（WARN）を足した。**`proposed` は現行でないので、孤児・逆孤児・
  `adr_not_landed` のどの検査からも見えない** —— 不変を `accepted` から始めた以上、
  下書きのまま置かれたものを誰かが見る必要がある。閾値は下書きと同じつまみを使い、
  新しいつまみを配らない。木に `proposed` は一件も無いので所見は増えない（実測）。
  これで RFC #159 第3節（連続的な昇格）が、**台帳もイベントも reducer も無しで**入る。
- 検査の件数を散文に書くのをやめた。ICD-005 と SPEC-011 に「32 検査／34 個」と
  書いてあったが、**検査が増えても誰も直さないので実際にずれていた**（測ると 36 個）。
  数は `checks_run` から導く。

### 決めた（外部レビューの RFC への裁き）

- **位置づけ（`status`）の正本を平文のフロントマターに留め、追記専用の台帳から導出しない**
  ことを決めた(ADR-093)。非目標 第18項として記録した（17項 → 18項）。
  RFC の背骨を断った判断であり、理由は測ったものを含む。
  **台帳は証拠を作らず、主張の置き場所を移すだけである** —— `status: current` と手で書けるなら
  イベントの一行も手で書ける。イベントを申告より強く信じられるのは verifier の同一性が
  attest されているときだけで、信頼根を持てない体系（確定事実7）では attest が名前だけになる。
  しかも**追記器は迂回できる**（非目標 第4項。2026-08-02 に二度実証した）—— 迂回できる台帳は、
  文書と静かに食い違うぶんフロントマターより悪い。
- **測ったところ、台帳が解こうとしている性質は既に証拠で縛られていた** —— 監査の 36 検査のうち
  **11 件**が「申告と実態の一致」を見ている（`adr_not_landed`・`stale_current`・
  `review_by_overrun`・`stale_draft`・`reverse_orphan_*`・`trace_stale`・`trace_missing_impl`・
  `source_drift`・`view_stale`・`projection_drift`）。RFC 第5節の「係留」も部分的に在る
  （`実装の指紋` と試験の走りの `PROVENANCE:`）。
- 影響範囲も測った: `status` をフロントマターから読む箇所は **34 箇所・8 ファイル**、加えて
  呼び手の実装と全文書、確定事実3 の置換。**払う費用に対して、得るのは申告の置き場所が
  変わることである。** そして平文が正本であることは `REQ-000` の値打ちそのものである。
- **永久の禁止ではない。** 再検討の条件を ADR-093 に書いた（attestation の信頼根が持てたとき）。

### 直した（門が自分で壊れる形）

- **2026-08-03 に `main` が赤くなった。誰も何も変えておらず、日付が進んだだけである**(ADR-094)。
  注入の試験が要約の `generated_at` を前の固定日に置き、呼び出しに時計を渡していなかったので、
  7 日が経った時点で鮮度の警告が湧いた。原因は二つ重なっており、切り分けて実測した ——
  **時計の固定だけでも通り、アサートの絞り込みだけでも通り、どちらも無いと落ちる**。
  絞り込みの側は、停滞の一行と鮮度の警告が**同じ部分文字列を含む**ための取り違えだった
  （WATCH-001 第10項と同じ形）。
- 規約は既に在って、正本と機械の検めが無かった —— `test_integration_e2e.py` の説明文に
  「実時計を使わない」と書かれていたが、**一つのファイルの説明文は他のファイルを縛らない**。
  メタの受入へ歯止めを置いた。**判定はクラス単位で、同じモジュール内の基底クラスまで辿る**
  —— 最初はファイル単位で書いたが、**落ちた当日の `main` でも通ってしまうことを実測した**
  ので作り直した（同じファイルの別のクラスが `--today` を 3 箇所で使っていた）。
  歯止めの実効を実測: `main` の版を戻すと `test_inject.py::TestTraceCoverageLine` を
  名指しして落ち、直した版で通った。免除の一覧は持たない（口の有無で対象を決める）。

### 足した

- ドメインの**種類**の語彙(`core`・`supporting`・`generic`)を登録簿へ置き、省略可の項
  `subdomain` を認めた(ADR-092)。**測ったところ、三種類はすでに書かれていて散らばっていた**
  —— 呼び手の木では、中核が最上位の要求と一本の仕様に、補完が三本の仕様に、そして**一般が
  非目標の文書**（「統治の規則を持たない」「図を描かない」）と上流へ委ねる決定に在った。
  しかも呼び手の非目標の文書は、報告と同じ本の §3.3.3 を根拠に引いていた。
  **一般は項では表せない** —— 作らないと決めたものに文書は無く、付ける先が無い。だから項は
  標識に留め、投資の判断は決定と非目標が持ち続ける。新しい型は足していない(20 型のまま)。
  用語辞書も触っていない —— `コア` を禁止同義語にすると**「共有コア」19 箇所が鳴る**
  （長い語に含まれる部分文字列の取り違え。WATCH-001 第2項が戻すなと定めた形）。
- 報告の提案のうち二つを**却下した**。(1) 同じ `domain` の中の種類の食い違いを検査する案は、
  **呼び手の木がドメイン一つに三種類を含む**ので、正しい状態を疑わしいと言うことになる。
  (2) 種類に `review_by` と再点検周期と期限切れの検査を課す案は、**変遷が既に追えていた**ので
  要らない —— 呼び手で中核が入れ替わったとき 8 文書が非現行へ移り、6 件が後継を指した。
  **文書で表せば遷移の機構がそのまま効き、項で表すと効かない**（項の上書きは機械に見えない）。
- 呼び手の木で実物に当てて確かめた: 66 節点すべてが項を持ち、未分類は空文字で返り、
  **所見は一件も増えない**（基準の上流と前後比較）。報告が引く Context Mapper の綴り
  `CORE_DOMAIN` を門が捕まえ、グラフは黙って捨てずに運ぶ。

- 製品の粒度の要求を `_system/` へ置けるようにした(ADR-091)。**測ったところ、内容は在って
  木の外にしか無かった** —— `README.md` の「何を解決するか」（ビュー。刻印で参照時点を示す型で、
  実際に古びたことが `WATCH-001` に記録されている）と `spec/doctrine.ja.md`（EXT アンカー。
  中身は写さない規律）だけである。**製品の目的は、この体系が唯一「外を指して済ませている」もの
  だった。**
  `_system/` は既に横断の三本（`DECIDED`・`NONGOAL`・`WATCH`）を置く棚であり、**棚は在って
  `REQ` が乗れなかっただけ**だった。型は増やしていない（20 型のまま）。粒度は置き場所が言う。
  この体系自身の `REQ-000`（この体系が解くこと）を、`README` の記述から移して書いた
  —— **使わない仕掛けを足さない**。常時投入には入れない（上限への影響を測っていない）。
- `_system/` の `REQ` を逆孤児の検査から外した。当初は「要求は要求が実現してよい」として
  判定を広げようとしたが、**実装してできないことが分かった** —— 越境依存のガードが
  「`_system` の ICD 宛にしてください」と拒み、`_system` に ICD は無い。実測でも `_system` の
  正本を frontmatter で指している文書は一件も無かった。**横断の正本は本文で参照され、辺では
  指されない**のがこの体系の規約であり、設計をその事実へ合わせた。
- 置き場所の規則を手書きの期待表で凍らせた(ADR-060 の様式)。`TYPES`・既定 `status`・既定
  `llm_context` は凍結済みだったのに、**置き場所だけが凍結されていなかった**（`REQ` の置き場所
  を変えても何も落ちなかったことで判明）。

- 雛形が定める節を全型で検めるようになった(ADR-090)。以前は `SPEC` の四節だけで、他の 15 型は
  雛形が定めるのに誰も咎めなかった。フロントマターは必須キーで守られているのに、本文の側に
  対応するものが無かった。**節の一覧はリンタの中に在り**、確定事実1（構造規則は
  `_registry.py` に一度だけ）に反していたので、登録簿へ移した。
  `SPEC_MISSING_SECTION`／`SPEC_EMPTY_SECTION` は一般形の `MISSING_SECTION`／
  `EMPTY_SECTION` に置き換えた（文言が型を名指す）。
- **一覧は実態を測って決めた。** 統治木 203 文書で雛形と本文を突き合わせ、**ある型の文書が
  100% ずれているなら雛形が誤り**と判じた。
  - `CHANGE`: 六件すべてが『理由』と『要求元』を `理由（要求元）` に統合していた → 雛形を統合。
  - `RESEARCH`: 四件すべてが主題ごとの見出しで書かれ、出所と日付はフロントマターに在った
    → **節を課さない型**にした（調査は探索で、形が先に決まらない）。
  雛形どおり強制する案は却下した —— 十件の所見がすべて「雛形が実態と違う」を意味し、
  **門が邪魔者になれば切られる**。それは門が無いより悪い。
- 残った真の欠け 4 件（`CHANGE-006`・`IMPACT-006`・`PROC-001`・`TEST-028`）を直した。
  **`TEST-028` は同日に私が書いた文書で、この検査が無いあいだに退行観点と合否基準を落として
  いた** —— 検査に値がある証拠として記録する。

### 直した

- 節点のスカラ項が入れ物に対して Python の内部表記を返していた。`title: [t]` が
  `"['t']"` として問い合わせに載っていた(ADR-092)。**一項ではなく、スカラ項すべてが通る
  共有の補助の欠陥**だったので、そこで直した。題名でも `llm_context` でも起きていた。
- コードの走査が `.gitignore` を見るようになった(ADR-089)。ビルドの投影に写った印まで
  範囲として返っていた —— 実測（呼び手のリポジトリ）で **30 件のうち 12 件（40%）が写し**
  だった。件数が 1.67 倍に狂い、正本を直すあいだ写しの側も指紋の食い違いとして立ち、
  **同じ一つの変更が二件に見えた**。
  判定は**走査の入口で一度だけ git に訊く**（`git ls-files -o -i --exclude-standard
  --directory`）。**`.gitignore` を自前で解釈しない** —— 否定・`**`・アンカー・親からの継承・
  `.git/info/exclude` を再実装すれば必ずどこかで git と違う判定を出し、「見ているつもりで
  見ていない」新しい欠陥になる。正本を再実装しない。
  git が使えない木では、勘定に状態を残して**いまと同じ挙動へ退く**（所見は出さない。git を
  使わない木を毎回叱らない）。追跡下のファイルは `.gitignore` に一致しても走査する。
  実測: 呼び手の木で写しが 12 件 → **0 件**。`trace_exempt` から `out/`・`dist/` を外しても
  同じで、**同じ事実を二箇所に持つ形が消えた**。
- 除外規則の一覧を手書きの期待表で凍らせた(ADR-060 の様式)。以前は枠の一致（勘定の鍵 ==
  正本表）だけを見ており、**規則を足しても黙って通った**（ADR-089 で二規則を足したとき何も
  落ちなかったことで判明）。`SPEC-026` の「TEST-026 が凍結する」は枠の一致を指すに過ぎなかった。

- `classify_edges` が両端から書かれた同じ事実に印を付けるようになった(ADR-088)。
  `A impacts B` と `B depends_on A` は同じ一つの事実を両端から書いたものだが、印が無いため
  読み手が図に描くと**循環に見えた**。利用者の実際の指摘 ——「依存しあっているものは異常？」。
  **本当の循環は 0 件だった。画面が作った嘘を、利用者が正しく読んだ。**
  端に `mirrored`（真偽）を足した。`kind` は据え置き（越境と dangling の分類とは別の軸なので
  混ぜない）。`depends_on` と `impacts` は引き続き別々の端として返す（畳まない）。
  読み手が `[src, dst]` を並べる自前の規則を持たなくてよくなる —— **畳み方の規則を読み手に
  発明させない。** 片方だけ書かれた端は咎めない（両端に書く義務は無い）。
  実測: 呼び手の木で辺の 28%（10 対 = 20 本）が両端書きで、印が自前の鍵と完全に一致した。

- `dep-graph.py --json` の節点が `title` を返していなかった(ADR-087)。**題名は必須項であり、
  体系が全文書に持たせている** —— 取れないのではなく返していなかった。原因は二段で、組み立てが
  題名を集めておらず、直列化が八項の白名簿で絞っていた。`superseded_by`・`updated`・
  `review_by`・`llm_context` も白名簿で落ちていた。
  **白名簿には正本が無かった。** `SPEC-006` は問い合わせの戻り値を定めていたが `--json` の
  節点の形をどこにも書いておらず、組み立てと別々に手で保つ形になっていて、実際にずれた。
  直列化の白名簿を廃し、**組み立てた項をそのまま返す**形にした（唯一の例外は
  `depends_on`／`impacts` で、生の値ではなく索引の値を返す）。節点の形を `SPEC-006` へ書き、
  **組み立てと直列化の項が一致することを受入で凍らせた**（正本を書いても、一致を機械が
  見ていなければまたずれる）。

### 足した

- 唯一の見張りである期限に、二段の要求を課した(ADR-086)。外部アンカー(EXT)は四本あり、
  **三本は機械が常時見張っている**(`exists`／`hash`)。`EXT-001` だけが `検査: review_by のみ`
  で、機械は何も見ていない。にもかかわらず四本すべてが同じ期限（更新から約3ヶ月）を持って
  いた —— **唯一の見張りである一本が、形式に過ぎない三本と同じ寛容さを与えられていた。**
  監査に `ext_sole_guard_missing`（期限が無い = 境界が沈黙して開いている。error）と
  `ext_sole_guard_loose`（更新から30日超 = 唯一の見張りが緩い。warn）を足した。
  段を分けたのは、見張りの不在が事実で機械で判じられる一方、30 日という数は見立てだから
  である。常時見張られるアンカーには課さない。
  実測: 2026-08-02 の一日で `EXT-001` の指す仕様について三件の反証が出た(ADR-077・078・080)。
  `ADR-032` は「実行環境が変わったら EXT-001 の再検証で拾う」と自ら書いていたが、拾ったのは
  定例ではなく外部レビューで、期日の約3ヶ月前だった。**定例は一度も先に捉えていない。**
- `EXT-001` の `updated` を 2026-08-02 へ、`review_by` を 2026-09-01（30日）へ改めた。
  今日 ADR-077・078・080 で本文を書き換えたのに `updated` を上げていなかった取り落ちも直した。

- 試験走行が判定の依り所を刷るようになった(ADR-085・SPEC-028)。走行の末尾に
  `PROVENANCE:` の節が並ぶ —— Python の版と実装・実行環境・プラグインの版・
  **`core.fileMode`**・対象の commit。
  2026-08-02、`_hookio.py` の実行ビットは手元で 1036 件すべて緑、CI では落ちた
  （`core.fileMode=false` により git の索引の mode が 644）。**「試験が通った」という主張が
  どの環境の話なのかを持っていなかった** —— JERG-2-610C の定義（検証とは客観的証拠を提示
  することによる確認）でいえば、それは検証ではなく検証戦略だった。
  証跡は**保存しない**（導出値を保存しない。ADR-055）。走らせた場のログが持つ。統治対象の
  内容（パス・作業ディレクトリ・統治木の名前）は刷らない。

- 被覆マトリクスに「実効の証」の列を足した(ADR-084)。各行は、その経路が効くことを示す試験を
  名指すか、示せない理由を添えて `未証` と書く。受入は名指された試験が**実在すること**を検め、
  `未証` の行の集合を凍らせる（**黙って増やせない**）。
  以前この門は文字の有無しか見ておらず、R12 の「圧縮前の促し」は欄が埋まったまま一度も
  届いていなかった(ADR-077)。**これは実効の証明ではない** —— 機械が検めるのは腐り（消えた
  試験を指し続ける行）だけで、効くのは著述の時点で「実効を示す試験はどれか」と問いが立つ
  ことである。強さを偽らないことがこの決定の一部である。

### 直した

- 用語チェッカーが報告する行番号が、フロントマターの行数だけずれていた(ADR-083)。行は本文内で
  数えられ、ファイルの行として提示されていた。**報告された行を開くと無関係な文が在るため、
  書き手は「誤検出だ」と判断して助言を捨てる** —— 正しい指摘を誤検出に見せる型である。
  実測(doctrine-lens の実在 ADR): 実在 85 行 / 報告 72 行、ずれ 13 = フロントマターの行数。
  換算を共有コアの出口に置いたので、将来リンタが行を運び始めても何もしなくても正しい。
  **単体試験では出なかった欠陥である**（見本のフロントマターが短く、行番号を検めていなかった）。

- 門が正しいものを咎める誤検出を二件直した(ADR-082)。どちらも `WATCH-001` 第2項が記録して
  いる取り違えと同型で、語の途中で切れる一致を咎めていた。
  - 用語チェッカーが、雛形が定める ADR の節見出し「却下した選択肢」の語を咎めていた。その
    前半を禁止同義語に持つ体系では、**雛形どおりに書いた ADR がすべて咎められて**いた
    (doctrine-lens の既存 ADR も同様)。`入出力` と同じく覆いの一覧へ加えた。
  - リンタの `RESEARCH_HAS_DECISION` が、見出しに『決定』を含むだけで咎めていた。
    `## 決定的な一点：…` が決定を書いたものとして扱われた。語を別語にする接尾
    (`決定的`・`決定論`)を否定の先読みで除いた。『決定』を主題にした見出しは引き続き咎める。
  取りこぼしは許すが誤警報は許さない(非目標2)。門が信用されなくなれば、本物の指摘も届かない。

- 確定事実8 の後段が偽だったので置き換えた(ADR-080 が ADR-032 を置換)。「Hook の設定は
  セッション開始時に読み込んで以後固定する」は、現行の公式 settings 文書が明文で否定して
  いる —— **settings 由来の hooks（user・project・local・managed）はセッション中に live
  reload され、変更ごとに `ConfigChange` が発火する**。インストール済み plugin の hooks は
  セッション中は保持され、`/reload-plugins` で再読込される。挙動が層で違うので、確定事実も
  層で書き分けた。「設定を変えたら新しいセッションで確かめる」は plugin 層の運用である。
- 版の遅れの警告と段差の案内の文言を、層に即して精密化した。本プラグインの配線は plugin 層
  なので、セッション中は保持されるという性質は変わらない。
- 非目標に第16項を足した —— **セッションの途中で設定が差し替わったことは検知しない**。
  モデルが冒頭で読んだ契約と実際に発火するガードが食い違う窓が開きうるが、`ConfigChange` は
  配線しておらず機械は見ない。もともと開いていた穴が可視になったものである。

- Hook 事象の集合を数で凍らせるのをやめた(ADR-078)。packaging の受入が「事象はちょうど
  7 個」を集合の完全一致で主張しており、**実行環境の機能追加を取り込む改善がテスト破壊
  として現れて**いた（現行の公式仕様が定める事象は 30 個ある）。いまは必要な配線が在ること
  だけを検める —— 増やしても落ちず、減らせば落ちる。
- 非目標「サブエージェントへの契約注入はしない」の理由を差し替えた(ADR-079)。
  「（届かない）」は偽である。`SubagentStart` は在り、`additionalContext` を運ぶ。
  非目標が自ら書いた再検討の条件（差し込み口が生まれた時点で再検討する）は、**既に
  満たされていた**。範囲としての非目標は保ち、根拠を実行環境の制約から「子へ渡す文脈は
  呼び出し側が組む」という選択へ移した。
- `EXT-001` を事象名の列挙から**依存している能力**の記述へ書き換えた(ADR-078)。名前の
  列挙は `review_by` の周期より速く古びる。

- 圧縮を跨いだ捕捉が、実効のある経路の上に載った(ADR-077)。`PreCompact` で
  `additionalContext` に退避の指示を載せて返していたが、現行の公式仕様では
  `additionalContext` を運ぶ事象は決まっており `PreCompact` は含まれない。届かない「版」が
  あるのではなく、**構造上どの版でも届かない** —— R12 の「圧縮前の促し」は一度も発火した
  ことのない死んだ経路だった。いまは `PreCompact` が圧縮の印だけを残し、合図は届く事象
  （`SessionStart` の `source=compact`、または印が新しいとき）で圧縮の**後**に出す。

- ICD 依存の境界が Edit・MultiEdit でも書き込む前に立つようになった(ADR-076)。三ガードの
  うち Guard2 だけが Write のときしか事前に判じておらず、Edit・MultiEdit は PostToolUse の
  block に回していた。その根拠は「事前に全文を作れない」だったが、同じファイルの
  `_proposed_text` がそれを反証していた(Guard1 と Guard3 はどちらも事前判定でそれを使う)。
  PostToolUse の block は書き込みを巻き戻さないので、違反する依存が一度作業木へ書かれ、
  その間だけ不正な状態で残っていた。全文を組み立てられないときは、対象が統治文書なら
  拒否する(判定を持たないまま allow へ倒れる経路を作らない)。

### 版を上げる利用者への注意

- ICD 依存に違反する Edit・MultiEdit が、これまでの「書けてから警告」ではなく **拒否**
  される。違反はもともと直すべきものだが、拒否の時点が早まる。
- `old_string` が一致せず編集後の全文を組み立てられないとき、対象が統治文書なら拒否する。
  編集を見直すと通る。

## [0.8.0] — 2026-07-29

フック境界と実行環境の堅牢化(ADR-075)。外部の多面レビューが、全門が緑のまま素通りしていた
欠陥を実行環境との境界に見つけた。すべて実測で再現し、実測で塞いだ。予防の面が沈黙で開く
経路を断ち、per-turn の約束を構造で凍結し、配布物から開発機の残骸を除いた。

### 版を上げる利用者への注意

- セッション印の書き先が `${CLAUDE_PROJECT_DIR}/.claude/.cache` に移った。旧配置
  (`${CLAUDE_PLUGIN_ROOT}/.cache`)は読み口としてだけ残る。この版へ上げた直後の一度だけ、
  一度きりの促し(レビュー・記録の確認)が再び出ることがある。
- リンタが新しい ERROR `FRONTMATTER_SYNTAX` を出す。これまで黙って値を落としていた
  フロントマターの構文の誤りが表に出る。既存の文書に誤りがあれば CI が赤くなる。
- 削除安全ガードが `cd <dir> && rm` と `git -C <dir> rm` を見るようになった。`cd` の宛先を
  静的に決められない削除(変数・コマンド置換)は拒否する。絶対パスで書き直すと通る。
- 縮小構成(`hooks.level2.json`)に UserPromptSubmit・Stop・PreCompact が戻った。手で
  配線している導入先は、新しい配線を写すと R11/R12 が動く。

### 直した

- 共有の読み手が種別を検めるようになった。統治木に `.md` という名の名前付きパイプが
  一つあるだけで、PreToolUse のガードと全件監査が永久に止まっていた(実測)。
- 拒否が符号化で消えなくなった。`PYTHONIOENCODING` が非 UTF-8 の環境で、日本語の拒否理由が
  例外になり終了コード1で握りつぶされていた(= fail-open)。入力側も同じで、日本語を含む
  payload が読めず判定を持たないまま allow へ倒れていた。
- per-turn のリンタが兄弟文書を読まなくなった。`depends_on` を持つ文書を編集するたび依存
  グラフを丸ごと組んでおり、木 8000 文書で 0.53 秒の O(N)。NONGOAL 第5項に反していた。
  ドメインは置き場所の名前だけから解く(実測 0.11 秒、規模に依らず平坦)。
- 受入の門が実効になった。性能の門は存在しないファイルを測っており、合成文書に
  `depends_on` が無いため守るべき経路を一度も通っていなかった。いまは「開いた統治文書の
  件数」で凍結し、旧実装に対して落ちることを確かめてある。
- フロントマターの構文エラーに消費者ができた。解析器は誤りを返していたが読む者が
  どこにもおらず、`sources: [Issue #60]` が `['Issue']` に化けたまま全門を通っていた。
- ガードの判定が整形に左右されなくなった。`status:  current`(空白2つ)のような整形の
  揺れで降格の拒否が外れていた。編集は生の全文へ当てる。
- 倉庫の不変が リンク経由で破れなくなった(包含の判定を realpath へ)。
- 削除が対象の `status` を問わなくなった(削除は状態に依らず死リンクを作る)。
- 統治木の解決が親をたどるようになった。環境変数が無く作業ディレクトリが部分
  ディレクトリのとき、統治が黙って落ちていた(契約が 627 文字から 99 文字へ)。
- 配布物から開発機の残骸が消えた。導入実体へ別ワークスペース時代の監査要約と
  セッション印が複製されていた。同梱の試験も導入先で通るようになった(実測 1 failure /
  4 error → 0)。
- 監査が仮名・漢字に隣接する id 参照を拾うようになった(`\b` の境界の取りこぼし)。
- パックの上限が、覆える要求をゼロにする落とし方をしなくなり、勘定が実出力と揃った。
- 依存の閉包が廃止文書の本文を同梱しなくなった(仕様 §3.8)。
- リンタが根の案内と dot ディレクトリ配下へ ERROR を出さなくなった。従うと ADR-029 に
  反し、スラッシュコマンドの定義が壊れる要求だった。
- そのほか: 助言の描画のサニタイズ、SPEC 必須節の階層、設定の型検査、印の書き込みの
  競合、正規表現の破滅的後戻り、`run_tests.py` が 0 件を成功と読む件、整合点検が解析
  不能を「食い違い無し」と同一視する件。

### 追加

- 上位設計書(`spec/doctrine.ja.md`)の検査を `exists` から `hash` へ上げた(EXT-003)。
  統治木の外に在り、どの門にも掛からない唯一の正本が、実際に古びていた(§4.3 の
  スクリプト表から実在する8本が欠け、直近5件の ADR の機構が書かれていなかった)。
- リリースの門に配布物の衛生の検査を足した(SPEC-027)。
- 共有コア `_hookio`(フック境界の入出力)と `_frontmatter.read_text`(種別の門)。

## [0.7.0] — 2026-07-28

ビューと刻印(ADR-073)と、不具合の記録と承認を経た issue 報告の促し(ADR-074)。どちらも追加の機構であり、既存の緑の監査は緑のまま。

### 版を上げる利用者への注意

- 新しい分類「ビュー」と検査 `view_stale` は、分類の記録(`.md-intake`)に「ビュー」を書いた体系でだけ発火する。書かなければ挙動は変わらない。
- エラージャーナル(`.claude/.cache/doctrine-errors.jsonl`)はローカルの記録だけで、通信はしない。鼓動の促しに従う報告は任意で、投稿は必ず利用者の承認を経る。
- 用語辞書の運用正本を持つ体系では、「ビュー」「刻印」を承認語として自分の辞書へ足すと、同梱シードと揃う(シードからの成長は正しい。欠落は `glossary_seed_drift` が指す)。

### 追加

- 不具合の記録と、承認を経た issue 報告の促し(ADR-074): フックの入口5本(ガード・リンタ・注入・鼓動・監査)が握りつぶした実行時例外の要約を、ローカルのエラージャーナル(`.claude/.cache/doctrine-errors.jsonl`。git の追跡外・上限20件)へ最善努力で残す。記録は許可制 — 部品名・例外の型と plugin 内の発生位置・版・時刻だけで、例外の自由文は写さないため、統治対象の機密(リポジトリ名・パス・本文)は原理的に入らない。鼓動は記録があるとき、報告の手順(事象だけの下書き→全文を利用者に見せて承認→利用者の手元の gh で投稿→感謝を伝える)を1セッション1回促す。報告は任意で、記録ファイルの削除で促しは消える。スクリプトは通信しない(DECIDED 事実7 のまま)。自動送信はしない(NONGOAL 第15項)。受け側として issue の定型フォーム(事象レポート)を追加。

- ビューと刻印(ADR-073): 分類の記録(`.md-intake`)に第四の分類「ビュー」(正本をある時点で人向けに解釈した体系外の文書)を追加。ビューは刻印(`<!-- doctrine:view src=… as-of=… date=… refs=… -->`)で参照時点を必ず記す。検出は三層 — リンタが刻印の欠落を助言(`VIEW_MISSING_STAMP`)、監査の新検査 `view_stale` が欠落(warn)と参照先の古び(advisory)を挙げ、リリース整合の門が公開ビュー3件(README・plugin/README・CONTRIBUTING)の `as-of` と版番号の正本の一致を強制する。分類の照合は完全一致がプレフィクスに勝つように変更(一括分類の配下から一件だけ取り出せる)。承認語「ビュー」「刻印」を仕様 §1 と辞書シードへ追加(「ビュー」は投影の禁止同義語から独立の語へ)。契機は外部の独立レビュー(2026-07-28)が指した README の主張3件の古び(要求数・Hook イベント数・検査数) — 本変更で修正し、再発は上記の三層が防ぐ。

### 内部

- 自己適用: README・CONTRIBUTING・plugin/README をビューへ再分類し、初期の刻印を打った。README の「投影」自称(ADR-029 で撤回済み)の残存も「案内」へ修正。TEST-020 文書の「4 つの Hook」の古びも修正。
- 悉皆の後追い(ADR-073 の着地の取り残しの回収): 整合点検(`consistency-check`)の対象に「ビュー」登録を追加(刻印の欠落は警告助言であり食い違いではない)。docs-curate の分類手順書(external-md-intake)を四分類と刻印の義務へ更新し、ビュー/非文書の見分け(「正本が変わったら書き直しが要るか」)を明文化。鼓動の移行キャンペーン文にビューの出口を追加。注入・監査の督促文の「三分類」を「分類」へ。lint ICD の検出コード一覧に `VIEW_MISSING_STAMP`、IMPL-018 に `entry_for` の完全一致優先と `parse_view_stamp` を追記。仕様 §4.2 の監査列挙にビューの刻印を追加。

## [0.6.0] — 2026-07-28

リリース整合の門(ADR-071)と悉皆トレースモード(ADR-072)。どちらも自己適用または opt-in の機能であり、通常の導入先には挙動の変化がない。

### 版を上げる利用者への注意

- 新しい設定キー(`trace_mode`・`trace_exempt`)は前方寛容(知らない読み手は無視する)。設定しなければ何も変わらない。
- 監査は木の `_system/.context-config.json` を既定で読むようになった(明示の `--config` が優先)。既存の調整キーがこのファイルに在れば、以後は監査にも効く。

### 追加

- リリース整合の門(ADR-071): CI が、本ファイル先頭の版付き節と版番号の正本(plugin.json)の一致・日付の存在を毎回検め、pull request の差分が `plugin/` に触れるなら本ファイルにも触れることを検める(記録に値しない変更は題名の `[no-changelog]` で明示的に免除)。「未リリース」節を常設し、リリース手順を PROC-001 に正本化した。検査は自己適用の `scripts/release-check.py`(SPEC-027。配布物に含めない)。
- 悉皆トレースモード(ADR-072): opt-in の設定 `trace_mode: "exhaustive"` で、「印なし」が監査 warn 一件の残高(trace_unmarked_backlog)になる。注釈を書き込めない媒体(テンプレート・JSON・法的文書)の出口として、設定側の適用除外 `trace_exempt`({パス: 理由})を追加。仕様側の悉皆が済んだ体系には、鼓動がモードの存在を一度だけ中立に案内する。既定は現状のまま(通常の導入先に挙動の変化なし)。監査の設定は木の `_system/.context-config.json` から既定で読む(明示の `--config` が優先)。

### 内部

- 自己適用: 本リポジトリで悉皆モードを入れ、印なし 62 件をゼロへ(範囲の注釈 11 本・ファイル内 exempt 27 本・設定除外 24 件)。全ファイルが「結線済み」か「理由つき管理外」になった。

## [0.5.1] — 2026-07-28

監査系の内部整理(ADR-069 の移送の完走)と、自己適用リポジトリ向けの生存性検査の追加(ADR-070)。通常の導入先には挙動の変化がない。

### 追加

- 版の遅れの検出(ADR-070): 鼓動が、実行中のプラグインの版と、リポジトリ自身の `.claude-plugin/marketplace.json` が同名プラグインに宣言する版を照合し、食い違えば更新の言い方まで含めて助言する。判定は共有コア `_auditcache.version_lag` に一度だけ。マニフェストを持たない通常の導入先では黙る。ADR-066 の「版の切替」(セッション途中)と対で、「遅れたまま動き続ける」の死角を閉じる。

### 内部

- ADR-069 の移送(4/4): 肥大関数2本を分解 — `scan_tree`(173行)を刈り/収集/規則の梯子/分類の4補助+司令部へ、`_build_sections`(129行)を知識系/状態系の2補助へ(挙動・順序・tier は不変)。**code-audit advisory 8→0 — ADR-069 の進捗計が完走**。分解の途中、置換の終端が旧関数内の早期 return に部分一致して旧本体が死にコードとして残る誤りを code-audit 自身が検出し(193行と名指し)、切除した。
- ADR-069 の移送(3/4): 二重定義リテラル4件を正本一つへ — 要約 schema と状態ファイル名は共有コア `_auditcache` を参照、Context Map 投影の差し替え印は構造規則の家 `_registry`(CTXMAP_BEGIN/END)へ新設し描き手(render-projection)と読み手(docs-audit)の双方が参照(DECIDED-001 事実1)。advisory 6→2。
- ADR-069 の移送(2/4): 体系外 .md 系の検査を `_audit_stray.py` へ移送(docs-audit 1,397→1,273 行 — 上限 1,300 を切り、肥大 advisory が消えた。advisory 7→6)。整合点検スクリプトの台帳読取は共有コア `_intake` 直参照へ(ADR-024 の単一正本に沿う)。
- ADR-069 の移送(1/4): 追跡系の検査群と停滞計を `_audit_trace.py` へ移し、149 行の検査関数を 6 つの小関数に分解(docs-audit 1,611→1,397 行。code-audit の advisory 8→7)。検査の名前・重大度・checks_run・決定性は不変(凍結どおり)。

## [0.5.0] — 2026-07-28

コードと仕様の双方向トレースの完成(保存則・宣言・キャンペーン・自己適用)と、統治自身の生存性(発火の印・版の自己認知)、開発方法論の機械化残差(code-audit)。外部レビューの採用分と、導入・再導入シナリオの実測で見つけた欠陥の修正を含む。

### 版を上げる利用者への注意

- 新しい検査は advisory か「宣言した人にしか発火しない warn」だけであり、既存の緑の監査は緑のまま。要約・状態ファイルへのキー追加は前方寛容(読み手は知らないキーを無視する)。
- フックの配線と契約はセッション開始時に固定される。版を上げたら新しいセッションを開始する — 忘れても、次のセッションから鼓動が「版の切替」を検出して案内する(ADR-066)。
- 実行時の印(`.claude/.cache/hook-stamps`)が増える。scaffold 導入なら `.claude/.cache/` は追跡外の既定。手で導入した場合は自分の無視一覧を確かめる。

### 追加

- **変更フローの段階化**(ADR-051): `change-impact` の段数を、変更の規模ではなく**決定を含むか否か**で分ける。決定を含まない変更(誤字・言い回し・書式・リンクの張り替え・投影の描き直し)は、影響の列挙と更新順序だけを残し `CHANGE`・`IMPACT`・`ADR` を要さない。誤字の直しに全14段を課すと統治を迂回する動機になり、フックを通らない経路(NONGOAL 第4項)の穴を体系自身が広げるため。不変条件の強制は経路に依らない(ガードと監査はフックと CI の層にある)。
- **編集画面の表示層の制約**(ADR-052): 実装より先に三つを凍らせる — 表示層に限る(統治判断を持たない)・索引が無ければ黙る(推し量りで埋めない)・中核は CLI が持つ。仕様は注釈の書式(ADR-048)が定まってから書く。

- **コードと仕様の双方向トレース**(ADR-048 の段1〜4 を実装): コードに `doctrine:begin <id>` / `doctrine:end <id>` の対を打つと、その範囲の内容の指紋を追跡する。**言語ごとに手段を分けない** — 行注釈を書ける全ての言語で同じ一つの規則が効き、構文解析器を使わない(ADR-054)。索引はファイルに置かず問い合わせのたびに導出し、記録するのは人の確認だけとする(ADR-055。生成物を版管理に置かないので取り込み依頼が衝突しない)。記録する指紋に位置を含めないので、**コードを別のファイルへ移しても古びと判じない**。監査は五つの検査(印の対応付けの誤り・実在しない id・廃止済みの id・古び・記録があるのに範囲が無い)を足す。
- **追跡の検査は opt-in**(ADR-056): 仕様の文書が `## 実装の指紋` の節を持つときだけ効く。節を持つ仕様が一つも無ければコードの走査そのものを行わない。導入した初日に大量の警告を浴びせず、使っていない機能の費用も払わせない。
- **`trace-index.py`**: 仕様の側から「これを実装しているコードはどこか」を引く問い合わせ。
- **性能受入の門を確定**(ADR-047 の予告の実装): 合成 1,500 文書で「1 編集のフック対(ガード+リンタ)が 1 秒以内」を試験として凍結(test_perf_gate)。per-turn の経路に全件走査が紛れ込む退行を CI が捕まえる。「受入テストで詰める」と書かれたまま残っていた宣言のみ項目の解消。
- **監査の分割方針を凍結**(ADR-069): docs-audit(約1,600行・30検査)を「枠+検査モジュール群」へ分割する方針と、分割で**変えないもの**(検査の名前と重大度・checks_run・決定性・列挙添字の凍結・SPEC-011 の指紋範囲)を先に決定した。移送は一回一系統の小さな変更列で行い、code-audit の肥大 advisory の減少を進捗の証跡とする(動的読み込みは沈黙する検証器の面が生まれるため却下)。
- **自己適用: 全現行 SPEC のコード対応を宣言**(ADR-061/065/067 の実運用初回転): 未宣言 20 本を 0 にした — 15 本は契約の核(定数表・照合の正本・モジュール冒頭の契約要約)へ狭い印を打ち指紋を記録、5 本は理由つきで「コード対応なし」を宣言(技能=.md・雛形=生成物混入の危険・JSON 配線=注釈不可・配布構成・表そのもの)。テスト文書の三角(TEST-026⇔test_tracescan.py)の先導例も記録。勘定は 寄与 2→17・印なし 71→58 へ。紐づけキャンペーンの完了条件(undeclared 0)に到達し、無宣言の義務化(ADR-061 の保留)を裁ける前提が整った。
- **開発方法論の機械化残差 code-audit**(ADR-068、PROC-001): 方法論(TDD/DDD/OOP。ADR-047)の担保が文書層で止まっていた穴を、コード層の検算五種で埋めた — import 境界の三条(入口↛入口・共有コア↛入口・_registry は最下層。error で CI の門を閉じる)・二重定義リテラル(advisory)・関数/ファイルの肥大(advisory)・解析不能(error)。標準ライブラリの ast のみ。導入初日の実測: error 0(境界規律は現に守られていた)・advisory 8(棚卸しで見つけた重複4件と肥大4件がそのまま名指しされた)。開発規範は PROC-001 として統治木に正本化し、CONTRIBUTING.md は指すだけに痩せた。テスト文書がテストコードの指紋を記録してよいこと(三角の閉包)も規範に明記。
- **段階と版の自己認知**(ADR-066、NONGOAL 第14項): 注入がセッション冒頭に実行中の包みの版を印に刻み、鼓動が毎プロンプト「今の版」と突き合わせる — 食い違えば「版の切替。配線と契約は古いまま。新しいセッションを開始する」を告げる(「版を上げたら再起動」という暗記知識を促しに変換)。Level 2 のまま全件監査の実績が確認できた体系には、Level 3 への上げ方を含む中立の案内を一度だけ出し、印を残して以後黙る(昇格は人の判断のまま)。CI の敷設・稼働の検出は原理的にできないことを NONGOAL 第14項として明文化(感知できるのは CI が残した要約まで)。
- **紐づけキャンペーンと停滞の名指し**(ADR-065): 体系外 .md の移行キャンペーンが出す物を持たない体系で、鼓動が未宣言の仕様の先頭一件を毎セッション促す(三つの出口〔指紋の記録/コード対応なしの宣言/exempt〕つきの自己完結文。要約の spec_coverage.next_undeclared が種を運び、鼓動は全木を走査しない)。監査は直前の要約と比べて「印なし+未宣言の和が動かない監査の連続回数」(stagnation_streak)を数え、3 以上なら冒頭の進捗計が「進捗が N 回の監査で動いていない」と名指しする(所見にはしない — 合否不変)。初回導入の位相(文書の整理→紐づけの整理)が同じキャンペーン枠で順に回る。
- **統治外の宣言 exempt**(ADR-067): ファイル自身に「印の語 + exempt <理由>」の一行を書くと、勘定の第四項(明示管理外)に数えられる。保存則は「到達 = 寄与 + 印なし + 明示管理外 + Σ除外」へ。台帳ファイルは作らない(宣言がファイルの中に住むので移動しても消えず、第二の正本が生まれない)。宣言と範囲の印の同居は warn(trace_exempt_conflict。宣言者にのみ発火)で、実態(範囲)を優先して数える。揺れた宣言は疑い(ADR-059 の拡張)。一覧は trace-index --term exempt で導出。印なしが「まだ判断していないもの」だけを指すようになり、進捗計と停滞検出の分母が締まる。
- **三つの保証限界の明文化**(ADR-064、NONGOAL 第11〜13項): サブエージェントへの契約注入はしない(届かない。子へは llm-context-pack で文脈を組む)・id の改名/改番は追わない(正規の道は後継の作成と置換)・ブランチ並行の採番衝突は防がない(事後に先勝ちで裁き影を error で名指し)。保証しないのに保証しているように見えうる箇所を正本で閉じ、保証の地図を完結させた。
- **セッション冒頭の進捗計**(ADR-058 の完結): 監査要約に勘定があるとき、SessionStart の注入が「追跡: 印なし N、未宣言 SPEC M」を冒頭の監査行に添える。「触った所から紐づける」ラチェットの進捗が毎セッション目に入る。
- **印の無いコードへの紐づけ促し**(ADR-063): 追跡を使っている体系(直近の監査要約に勘定がある)で、印の無いコードを編集すると、促しの面(review-nudge)が紐づけの一行を**セッションに一度だけ**出す。両方の道(印の対+指紋の記録/「コード対応なし」の宣言)を示す。読むのは編集された一ファイルと要約だけ(ターン毎の全件走査はしない。NONGOAL 第5項)。追跡を使っていない・版を上げただけの利用者には何も出ない。
- **フックの発火の印と拒否経路の欠落検出**(ADR-062): 注入(SessionStart)・ガード(PreToolUse)・リンタ(PostToolUse)が起動のたびに `.claude/.cache/hook-stamps`(git の追跡外)へ印を残す。ガードとリンタは全段階の配線で同じ編集の出来事に対で結ばれ同じ包みで配られるため、「リンタの印だけが進む」食い違いは配線の欠落を意味する — 鼓動が毎セッション告げ、監査が advisory(guard_liveness_gap)で挙げる。判定は `_auditcache` に一度だけ(ADR-053 と同じ原理)。印が無ければ黙る(更新直後の前方寛容)。NONGOAL 第9項を「起動の証跡は取れるが、拒否の往復はカナリアの領分」へ狭めた(ADR-050 が予告した段階)。
- **仕様のコード対応の宣言**(ADR-061): `## 実装の指紋` の節に `- コード対応なし: <理由>` の明示宣言を書けるようにし、追跡から外れる道を「無音の節の削除」から「読める宣言」に変えた。宣言に反して範囲がコードにあれば warn(trace_unexpected_impl)、節の無い現行 SPEC を範囲が指せば advisory(trace_undeclared_impl。節を消して注釈だけ残る欠陥の可視化)。現行 SPEC の三分類(traced・no_code・undeclared)を勘定に載せる。無宣言の義務化はしない(数と advisory まで。義務化は別の ADR)。
- **検査の挙動を正本の列挙で添字づけた試験**(ADR-060): 状態×指紋一致の全 16 マスを、`ALL_STATUSES` を読み込んだ添字と手書きの期待表で凍結(状態が増えたら期待を書き足すまで試験が通らない)。`AUDIT_CHECKS` の「TEST が凍結する」という宣言を実在させ(独立転記の表と全量一致・`checks_run` と一致)、走査の所見コードに産出側の正本 `FINDING_CODES` を置いた。
- **綴りの揺れた印の検出**(ADR-059): 厳密な照合に一致しない行のうち、印の語の直後に begin/end が続くものを advisory(trace_marker_suspect)で挙げる。「打ったつもりの印」が無音で消える経路(実測で六変種すべて無音だった)を塞ぐ。厳密な照合は変えず、合否(error/warn)も変えない。あわせて、走査が告げた切り詰め所見を監査が握って捨てていた穴を閉じ、advisory(trace_scan_truncated)で転記する。
- **走査の保存則**(ADR-058): 走査が触れたファイルを必ず「寄与・印なし・規則による除外」のどれか一つに数え、勘定(件数)を監査の要約 `trace_coverage` に載せる(検査所見は増やさない — 既存の緑は緑のまま)。除外は正本の規則表 `EXCLUSION_RULES` を経由してだけ起こり、全規則の枠を 0 件でも出す(空欄を許さない。SPEC-025 と同じ原則)。内訳の一覧は `trace-index --coverage --term <項>` が求めに応じて導出する(保存しない。ADR-055)。印なしの件数は「触った所から紐づける」運用の進捗計になる。

### 修正

- **名前付きパイプで走査が永久に停止する欠陥**(ADR-058): 大きさの検査(サイズ 0)を通過した後の `open` がブロックし、走査全体が戻らなかった(実測)。通常ファイル以外(名前付きパイプ・ソケット・デバイス)は開かずに除外へ数える。あわせて、読めないディレクトリ・降下しないシンボリックリンク・根と統治木が重なる呼び方の穴も、黙らず勘定に載る形へ閉じた。
- **再導入で前の世代の健全さが引き継がれる欠陥**(ADR-053): 統治木を消して同じ場所に作り直すと、前の世代の監査要約の `root` が一致してしまい、一度も監査していない木に「error 0」が注入され鼓動も黙っていた(実測では、新しい木が実際に error を持っていてもそうなった)。統治木の `initialized` の日で世代を判じ、それより古い要約は捨てる。粒度は日で、同じ日の作り直しは捨てられない(既知の限界)。
- **監査要約の読み手ごとに答えが割れる欠陥**(ADR-053): 注入と鼓動で `schema` 照合の段が違い、未知のスキーマの候補が先にあると一方だけがそこで止まっていた。読み取りを共有コア `_auditcache.py` に一本化し、条件を満たさない候補は止まらず次の候補へ進む。
- **重複 `id` の採用規則が二つあった欠陥**(ADR-049): 注入は先勝ち、グラフと監査は後勝ちで、監査が「採用」と告げる文書と契約が運ぶ文書が食い違っていた。規則を `_registry.resolve_duplicate_id` に一本化し**先勝ち**へ統一(後から来た衝突が、既に id を持つ文書からそれを奪わない)。

### 変更

- **ガード経路の死活を保証しないことを明記**(ADR-050、NONGOAL 第9項): 生存性(R11)が見ているのは監査の要約の鮮度と定例の期限までであり、ガードが今も拒否できることの証跡ではない。配線の欠落で SessionStart だけが動く構成では、注入も要約も健全なまま拒否だけが消えていることがありうる。機能カナリアは版を分け、入った時点で第9項を狭める。

### 内部

- 導入シナリオの実測(新規・導入直後・成熟リポジトリへの後入れ・成熟途中・アンインストール残骸・再導入・版上げ・別プロジェクトの cache)で、非破壊・前方寛容・沈黙境界を確認。
- 検証: テスト全緑 873 件・監査 error=0 warn=0 advisory=0・linter⇔audit 整合・投影ドリフト 0。

## [0.4.0] — 2026-07-26

実機(導入済みプラグイン)での全数検証と、その中で見つけた欠陥の修正、および移行キャンペーン(Issue #60)とメモリ境界の実装。

### 追加

- **移行キャンペーン**(ADR-034、Issue #60): 途中導入で残る体系外 .md を、1鼓動1件で片づける。未分類の一覧は監査から毎回導出し、進捗の正本は `.md-intake` だけ(二重の台帳を作らない)。`gov-heartbeat.py` が統治率つきの自己完結文で先頭1件を促し、SessionStart の注入が残数を常時表示する。優先の見立て(被参照・git の活動量)は docs-curate の手順に明記。
- **メモリの影の見張り**(ADR-035): ハーネスのメモリに書いてよいのは環境と個人の事実だけ、と DECIDED 事実9 として常時注入で届ける。監査の `memory_shadow` 検査(計18検査)が、メモリの .md が統治文書へ言及していたら正本との矛盾点検を促す(中身の判定はしない。§7)。
- 実機検証: 導入済みプラグイン経由で 7 イベント全てを実エンベロープで駆動(SessionStart 0.55秒・ガード deny 0.05秒・PostToolUse 三連 1.15秒)。半年後の基準日で全現行文書が老化シグナルを持つこと(warn 40 件)と、検知→要約→注入→促しの連鎖を通しで実証。

### 修正

- **Level 2 での監査死活の誤報**: SessionEnd の監査が無い Level 2 で「監査が動いていない」と警告していた。heartbeat と注入の死活検査を Level 3 以上に限定(定例・移行の督促は全 Level のまま。ADR-030 の精密化)。
- **監査要約キャッシュをプロジェクトスコープへ**: 書き出し先を `${CLAUDE_PROJECT_DIR}/.claude/.cache/last-audit.json` に変更。プラグインの更新で失われず、同じプラグインを使う別プロジェクトと衝突しない(読み手はプラグイン側 cache も後方互換で読む)。
- 使い始めの前の定例督促を抑止(騒がしい導入にしない)。SPEC-012 の重複行を整理。

### 内部

- `_system/.governance-state` を新設し、初回の定例レビューを実施・記録(near_duplicate 0・canonical_conflict 0・review_by_overrun 0)。
- 検証: テスト全緑(新規 12 件)・監査 error=0 warn=0 advisory=0(18検査)・linter⇔audit 整合・投影ドリフト 0・term-check 0。

## [0.3.0] — 2026-07-26

全体批判レビュー(2026-07-26)の全所見(重大12・中18・軽微18)への対応。主題は「自走しない統治」からの脱却 — 発火面の全域化と、統治自身の死活の可視化。

### 追加

- **R11 統治の生存性・R12 会話知識の捕捉を要求化**(REQ-014/REQ-015、spec §2)。
- **Hook を 7 イベントへ**(ADR-028): `gov-heartbeat.py`(UserPromptSubmit。監査の鮮度と doc-review 定例の期限をセッションに一度督促)・`capture-nudge.py`(Stop。記録なきセッションの終端を一度だけ差し止め)・`precompact-dump.py`(PreCompact。圧縮前に未記録の決定を `_system/.session-notes` へ退避させ、次セッションが選別を義務化)。三つとも Level に依らず動く(ADR-030)。
- **監査に 6 検査を追加**(計17): 陳腐化の疑い(型既定点検周期。ADR-025)・上流更新の伝播・アーカイブ整合(ADR-027)・決定の着地(adr_not_landed。「文書上の宣言に留まる」欠陥類型の機械化)・辞書シードの退行・外部アンカーの存在(ADR-026)。
- **EXT 型**(ADR-026): 統治木の外への依存(設定・上位設計書・CI・実行環境の仕様)をアンカーとして登録し、存在を監査が見張る。「畿外の中身は統治しない、畿外への依存は統治する」。
- **被覆マトリクス**(SPEC-025): 統治要求×発火経路×証跡の一覧をテストで凍結。空白のセルを構造的に禁じる。
- 導入先向け CI 雛形(`docs-system-init/references/ci-example.md`)。

### 修正

- **自己適用の全停止を修復**: marketplace のパスが改名前の場所を指したまま、全フックが 4 日間沈黙していた。パス修復に加え、「前回監査なし/古い」を死活の警告へ格上げし、CLAUDE.md/AGENTS.md に統治の生存期待を明記(R11)。
- **カルクの書き戻し先を運用正本(`_system/glossary.md`)へ統一**(5箇所が機械の読まない spec §1 を指していた)。辞書の階層(運用正本が機械照合の正本、spec §1 とテンプレートはシード)を明文化。
- **doc-review 定例の周期を実体化**: 実施記録は `_system/.governance-state`、督促は heartbeat(cadence-review.md の誤引用を解消)。
- **ADR の着地漏れを解消**: ADR-005/007/008/011/012/014/015/016 を SPEC/ICD/NONGOAL から引用。ADR-014 の帰結(DECIDED 混入の定例点検)を docs-curate に実装。WATCH 第6項に欠陥類型を登録。
- **decided-facts の根拠不在を解消**: 事実3/7/8 の根拠 ADR が実在しなかったため、ADR-031(標準ライブラリのみ)/ADR-032(Hook スナップショット)/ADR-033(必須8キー)を追認起案して張り替え。
- **アーカイブ整合**(ADR-027): status archived ⇔ `<domain>/archive/` をリンタ・監査・不変ガード(状態でも拒否)で機械化。RESEARCH-001 を `model/archive/` へ移送。
- regression-guard の description を引用符で囲む(厳格 YAML での解析失敗を防止)。5 技能へ日本語トリガーを追加。3 技能の裸名スクリプト参照に `${CLAUDE_PLUGIN_ROOT}` を付与。docs-curate の Level 要求を作業別に分割(促しと拒否の往復を解消)。external-md-intake の統治木ハードコードを解消。投影の `--check` の正しい使用法と Context Map の印(手編集の境界)を明記。CLAUDE.md/AGENTS.md を「案内」に再定義(ADR-029)。
- 0.2.1 の本履歴にある「10 セッションごと」は、その後の変更で「会話 10 回ごと」になった(履歴は当時のまま残す)。

### 内部

- spec/doctrine.ja.md を現行決定(ADR-023..033)へ整合し `status: current` に。型は 20(EXT 追加)、テンプレートは 21。
- 統治木に 28 文書を追加(ADR 9・REQ 2・SPEC 5・TEST 5・IMPL 3・EXT 4)、投影を再描画。
- 検証: 753 テスト緑・監査 error=0 warn=0 advisory=0(17検査)・linter⇔audit 整合・投影ドリフト 0・term-check 0。

## [0.2.1] — 2026-07-22

### 修正

- **リンタが正規の非文書へ ERROR を出し続ける欠陥を解消（ADR-024）。**
  監査（docs-audit）が「非文書／投影」と認めた .md（README・各 skill の SKILL.md・参照散文など実測 37 件）や、統治木の根に到達できない体系外のファイル（ハーネスのメモリなど）に、リンタ（docs-linter）が編集のたび `MISSING_FRONTMATTER` 等の ERROR を出していた。監査（全体を見る）とリンタ（一件を見る）の判定が食い違い、正規状態が恒常的に「要修正」と報告されて、警報の信頼が摩耗し、エージェントを `.txt` 改名などの設計違反の回避へ誘導していた。
  - リンタは判定の前に統治木を探し、次の順で扱う。①統治木が無ければ（体系外）何も出さない。②型付きは従来どおり全点検（統治木の外なら `STRAY_DOCUMENT`）。③型なしで intake に「非文書／投影」と登録されたファイルは schema/frontmatter を飛ばし用語助言のみ（WARN）。④それ以外は従来どおり。

### 追加

- **共有コア `plugin/scripts/_intake.py`。** 分類の記録（`.md-intake`）の読み取りを監査とリンタで一本化。二つの道具が同じコードで同じ記録を読むことで、判定の食い違いを構造的に防ぐ。
- **整合点検 `scripts/consistency-check.py`。** 監査が非文書と認めたファイルにリンタが ERROR を出さないことを機械で確かめる回帰ガード。WATCH-001 に第 5 項として登録。
- **整合点検の横断リマインダ `scripts/consistency-reminder.py`。** セッションを跨ぐカウンタで 10 セッションごとに点検を促す（`.claude/settings.local.json` に配線、`/consistency-check` で手動実行）。

### 内部

- SPEC-007（§制約・§エラー時挙動を条件化）・IMPL-007・TEST-007 を決定に合わせて更新。`test_linter.py` に受入基準①〜④のテストを追加。
- 監査（docs-audit）の intake 読み取りを共有コアへ再配線（挙動は不変）。
- 検証: 全 710 テスト緑・監査 error=0・投影ドリフトなし・整合点検 緑。

## [0.2.0] — 2026-07-07

- 全体監査所見の一括解消。統治木の既定名を `doctrine_docs` へ（ADR-022）、体系外 .md の分類の記録（ADR-021）、PROC 型と統治境界（ADR-013〜017）、固有名の動的な覆い（ADR-018）、段差の自己ゲート（ADR-019）。プラグインの自己適用（実行時ドッグフード）を開始。
